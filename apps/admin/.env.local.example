# 复制为 .env.local 后按需修改。
# 后端地址：docker compose 默认 8000；tools/local-verify/run-smoke.ps1 用的是 8123。
NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1

# 路由守卫的"提示位" cookie 名（非 httpOnly，只放一个 1/0 标志，不含任何凭据）。
# 见 apps/admin/docs/B端联调坑.md 第 2 条。
NEXT_PUBLIC_AUTH_HINT_COOKIE=yj_authed
