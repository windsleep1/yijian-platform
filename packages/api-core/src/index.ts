/**
 * `@yijian/api-core` —— 前端跨 app 共享的**纯逻辑**（无 React、无 DOM、零依赖）。
 *
 * 目前只有认证链路一个模块，**刻意保持这么小**：它是"两端必须一致"的那部分，
 * 不是"可以复用的一切"。下一步要不要扩，判据 = 「同一个文件需要在两个 app 各改一次」
 * （见 `docs/22-C端-方案.md` §3.3）。
 */
export * from "./auth-chain.ts";
