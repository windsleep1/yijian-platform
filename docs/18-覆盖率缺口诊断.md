# 18 · 覆盖率缺口诊断（补测批 · 第一步）

> **本批只诊断，不改产品代码。** 唯一动过的是覆盖率**采集配置**与**诊断工具**
> —— 因为不先修它，产出的清单是假的（见 §1）。
>
> 数据口径：`tools/local-verify/run-smoke.ps1` 从零库跑 127 条用例（126 passed / 1 skipped）。
> 分类清单由 `tools/local-verify/cov-gaps.py` 机械生成（按"连续缺失区域 + 所在函数"分组），
> 类别与说明由人逐条判定。
>
> 产出日期：2026-09-21

---

## 0. 一句话结论

**`services` 层不是 49.6%，是 86.6%。** 原报告的 1476 条缺口里，
**1087 条（74%）是测量假象**，来自三处**互不相干**的采集边界问题。

真实待办 = **365 条**。

> ⚠️ **2026-09-21 复算更正**：本文初版这里写的是「A 354 / **B 7** / C 4」——**`B 7` 是错的**。
> §4 总览说 7 条，§5.16 的清单逐项加起来是 10 条，两处自相矛盾（`deps.py:165` 被重复/漏算）。
> 机械复算（按行区间取 `analysis2` 的 missing）确认真实为 **10 条**。

| 类别（初判） | 条数 | 占比 | 处置 |
|---|---|---|---|
| **A** 该测的（错误分支 / 边界 / 权限拒绝 / 幂等） | **351** | 96.2% | 补测：构造反例 + 变异验证 |
| **B** 零引用 / 永不执行 | **10** | 2.7% | **逐条判断（§5.16）**：删 5 / 接上 4 / 改判 A 1 |
| **C** 等环境（加 pragma 并写明在等什么） | **4** | 1.1% | pragma + 说明 |
| （边界 #2 / #3 另计，不计入 365） | 190 + 24 | — | 见 §1 |

**B 类不是"一大批防御代码"，C 类也不是"一堆 pragma 场景"** —— 两者合计 14 条、占比 3.8%。
这说明本项目的历史问题不是"写了太多没人走的代码"，而是
**"写了正确的错误分支，但没有人去触发它"**。补测的方向因此非常单一。

**逐条判断的判决结果**（依据 §5.16 的表）：

| 判决 | 条数 | 涉及 |
|---|---|---|
| A 补测 | 351 | §5.1–5.15 |
| A′ 补测 **+ 补调用点** | 1 + 2 | `deps.py:165`（由 B 改判）；`deps.py:160-163`（`by="user"`） |
| B′ **保留代码、删掉本地副本** | 2 | `db/base.py:94-98`（与 `cli.py:45` 的副本逐字相同） |
| B **删除** | 5 | `_rule_item_from_json` 3 / `list_roles` 2（`_version_chain` 已 pragma，不进 579） |
| C pragma + 说明 | 4 | `main.py:62` / `sms_service:100` / `errors.py:100-101` |
| **合计** | **365** | ✓ |

---

## 1. 三处测量边界（本批最重要的产出）

这三处**同时存在**，而且症状完全不同，所以之前逐个排查时都被单独误判过。

| # | 症状 | 根因 | 规模 | 修法 | 状态 |
|---|---|---|---|---|---|
| **1** | 同一个函数里，**DB `await` 之后的行全部记成"未执行"**；同时把没走的分支记成走过 | SQLAlchemy async 的**每个** DB 调用都经 `greenlet_spawn()` **切一次 greenlet**；coverage 默认 `concurrency = thread` **不感知切栈** | **897 条** | `.coveragerc` 加 `[run] concurrency = greenlet` | ✅ **本批已修** |
| **2** | `app/cli.py` 记为 **190/190 全未覆盖**，而冒烟日志里它明明打印过 `[cli] RBAC seed replayed` | 覆盖率**只在 API 进程**采集；CLI 跑在**另一个进程**，且那个进程没有插桩 | **190 条** | ① 给 CLI 调用加 `coverage run` 插桩后 `coverage combine`；② 或把它移出门禁口径并写明理由 | ⏳ **待裁定** |
| **3** | `float_loss_report` / `assert_float_safe` 记为未覆盖，而用它们的用例 **PASSED** | 门禁只量 API 进程；**纯单元测试在 pytest 进程里直接调 app 代码**，那部分执行不在视野内 | **24 条** | 跑 `coverage run -m pytest`，再 `coverage combine` 两份数据 | ⏳ **待裁定** |

### 1.1 边界 #1 的实测证据（同进程、只改一个变量）

`tools/local-verify/_probe_greenlet.py` 用 ASGI 直驱（不经 uvicorn），
只改 `concurrency`，测两处**必然执行**的行：

| 配置 | `auth.py:76`（注册路由 return） | `deps.py:117`（取权限） | `deps.py` 90-130 覆盖行 | `auth_service.py` 覆盖行数 |
|---|---|---|---|---|
| 默认 `thread` | ❌ False | ❌ False | `…111,` **`121`**, `124` | 119 |
| **`greenlet`** | ✅ True | ✅ True | `…111,` **`112,114,117,118`**, `121,124` | **181** |

判据来源：用例断言的**就是** `auth.py:76` 的返回值；`deps.py:117` 在 `current_user` 里，
每个带鉴权的请求都要跑。**它们不可能没执行。**

注意那张表的形状 —— 这是根因的指纹：

```
deps.py:111  ✅ 有记录   ← 第一个 await（切栈**之前**）
deps.py:112-118 ❌ 全丢  ← 切回来之后，追踪器的"当前帧"已经指错
deps.py:121  ✅ 有记录   ← 模块级，另一个帧，不受影响
```

**为什么排错绕了远路**：默认 / `pytrace` / `ctrace` 三种追踪内核结果**完全一致**，
说明问题不在"用哪个内核"，而在"内核与 greenlet 栈切换没有对接"。
排错顺序的教训记在 `apps/admin/docs/B端联调坑.md` 坑 54。

⚠️ 还有一个容易漏掉的性质：**旧数据不是"偏低"，是"偏错"**。
`auth.py` 在旧口径下覆盖 **93** 行、新口径 **78** 行 —— 同样三个请求，
旧口径**多记了 15 行没执行的分支**。也就是说旧数字**两个方向都错**，
它既不能用来定门槛，也不能用来判断"哪里有缺口"。

### 1.2 边界 #2 / #3 的规模是"测出来的"，不是估的

- **#3**：把 `tests/test_idgen.py` + `test_admin_v4.py::test_content_hash_and_answer_derivation`
  在 pytest 进程里跑一遍，再合并两份数据 → **579 → 555（−24）**，
  其中 `core/idgen.py` 一条文件就 **28 → 6（−22）**。其余 20 个文件**一条不变**，
  这反过来证明"HTTP 用例确实都打在 API 进程里"，边界很干净。
- **#2**：`cli.py` 是 190 语句单文件 100% 缺失，占**全部缺口的 33%** ——
  这是目前**单笔最大的**账。

---

## 2. 修正后的真实分布

| 层 | 文件 | 语句 | 未覆盖 | 覆盖率 | 占全部缺口 |
|---|---|---|---|---|---|
| **services** | 10 | 2286 | 306 | **86.6%** | 52.8% |
| **(top)** `cli.py` / `main.py` | 3 | 222 | 192 | **13.5%** | 33.2% |
| core | 7 | 398 | 54 | 86.4% | 9.3% |
| schemas | 9 | 925 | 14 | 98.5% | 2.4% |
| db | 3 | 152 | 9 | 94.1% | 1.6% |
| api | 12 | 322 | 4 | 98.8% | 0.7% |
| **TOTAL** | **44** | **4305** | **579** | **86.55%** | |

对照三个口径：

| 口径 | 语句 | 未覆盖 | 覆盖率 |
|---|---|---|---|
| 修正前（已作废） | 4305 | 1476 | 65.64% |
| **门禁今天看到的**（API 进程） | 4305 | **579** | **86.55%** |
| 加上 pytest 进程（边界 #3 修复后） | 4305 | 555 | 87.11% |
| 再把 `cli.py` 移出口径（边界 #2 方案②） | 4115 | 365 | **91.13%** |

> **`services` 层 49.6% → 86.6%**：你那句"补测现在就能做"是对的，
> 但**目标定低了 26 个点** —— "≥ 60%" 这条线**本来就已越过**。

---

## 3. 分类判据（按顺序问，问到就停）

1. **它现在根本不会被任何代码调到吗？**
   —— 机械判定：AST 扫全仓求"零引用"，再对字面量 **grep 反查一次**（硬约定 F）。
   → **B（该删）**
2. **不改变部署形态 / 环境配置，靠"构造输入 + 注入故障"就能让它执行吗？**
   → **A（该测）**
3. **它要等到某个环境或部署形态才会执行吗？**
   （真实短信通道、生产环境告警、真故障兜底）
   → **C（该忽略，加 `pragma: no cover` 并写明"在等什么"）**

第 2 条里的"注入故障"是指：**依赖是显式传进来的参数**（如 `redis`、`db`），
测试可以直接传一个会抛异常的替身 —— 这属于构造输入，不属于改环境。

---

## 4. 分类总览

**口径声明**：下面的逐文件数字是**门禁口径（579 条）**，不是 `.coverage` 现盘的 555 条
（后者已合并 pytest 进程，见 §1.2）。两类不相容的字面量不要混用。

```
services/import_service.py      109 条   A ×109
services/exam_service.py         73 条   A ×70   B ×3      ← _rule_item_from_json
services/question_service.py     40 条   A ×38   （另 2 条属边界 #3）
services/rbac_service.py         27 条   A ×25   B ×2      ← list_roles
services/auth_service.py         19 条   A ×19
services/config_service.py       15 条   A ×15
services/sms_service.py          10 条   A ×9    C ×1
services/user_service.py          7 条   A ×7
services/audit_service.py         6 条   A ×6
core/idgen.py                    28 条   A ×6    （另 22 条属边界 #3）
core/deps.py                     11 条   A ×8    B ×3      ← by="user" 2 + 空 identity 守卫 1
core/errors.py                    7 条   A ×5    C ×2
core/security.py                  5 条   A ×5
core/config.py                    3 条   A ×3
db/base.py                        9 条   A ×7    B ×2      ← raw_asyncpg_connection
schemas/admin_question.py         7 条   A ×7
schemas/admin_exam.py             4 条   A ×4
schemas/auth.py                   3 条   A ×3
api/v1/auth.py                    2 条   A ×2    ← 短信登录整链路零覆盖（最高优先）
api/v1/admin_imports.py           2 条   A ×2
main.py                           2 条   A ×1    C ×1
cli.py                          190 条   —— 边界 #2，整文件级裁定（见 §5.17）
────────────────────────────────────────────────────────────
A  351 +  B  10 +  C  4  =  365      （另 边界 #3 = 24、边界 #2 = 190）
365 + 24 + 190 = 579 ✓
```

---

## 5. 逐条清单

### 5.1 `app/services/import_service.py` — 109 条（A ×109）

导入管道是本层最大的缺口，但缺口形状很集中：**`validate_row` 的逐字段校验分支几乎没被触发过**。

| 行号 | 类别 | 说明 |
|---|---|---|
| 111 | A | `parse_file`：文件内容为空 / 损坏 → `bad_request` |
| 119 | A | 不支持的文件类型（`.txt`）→ 40001 |
| 126-128 | A | 编码探测：所有候选编码都解不开 → "请另存为 UTF-8 后重试" |
| 135 | A | CSV 没有表头行 |
| 148-150 | A | JSON 解析失败 → 带行号的 `bad_request` |
| 151-158 | A | JSON 是对象时按 `questions`/`items`/`rows`/`data` **四个键**拆包；四个都不是数组 → `else` 分支 |
| 161 | A | JSON 数组里混进非对象元素 |
| 253-255 | A | `_s`：空值归一为 `""`；list/tuple 用 `|` 连接 |
| 265-266 | A | `_int_or_none`：非数字字符串 → `None` |
| 272 | A | `_float_or_none`：空值 → `None` |
| 275-276 | A | `_float_or_none`：非法输入 → `None` |
| 297-304 | A | `parse_answer_points` 主流程（`;;` 分段）**测试从没走过这个格式** |
| 307-312 | A | 同上：`|分值` / `分` 后缀 / 分值非法时 `except` 兜底 |
| 336-338 | A | `_build_answer`：`case` 题型单独分支 |
| 387 | A | `validate_row`：科目编码为空 |
| 391 | A | `validate_row`：科目编码不存在 |
| 414 | A | `validate_row`：与批次指定科目不一致 |
| 421 | A | `validate_row`：科目未过校验时无法校验章节 |
| 432 | A | `validate_row`：科目未过校验时无法校验知识点 |
| 436-438 | A | `validate_row`：知识点编码不存在 / 知识点不属于所填章节 |
| 443-445 | A | `validate_row`：题型为空 / 题型非法 |
| 450-456 | A | `validate_row`：题干为空 / 过短 / 过长 / 含 HTML 标签 |
| 466 | A | `validate_row`：判断题答案不是 A/B |
| 475 | A | `validate_row`：选项少于 `MIN_OPTIONS` |
| 478 | A | `validate_row`：客观题答案为空 |
| 489-491 | A | `validate_row`：单选正确项 ≠ 1 / 多选正确项 < 2 |
| 498-502 | A | `validate_row`：文本作答题型（填空/简答/案例）的分支 |
| 507-509 | A | `validate_row`：解析为空 / 过短 |
| 514 | A | `validate_row`：分值 ≤ 0 |
| 517-519 | A | `validate_row`：难度非整数 / 越界 |
| 524 | A | `validate_row`：年份越界（1990~2100） |
| 529 | A | `validate_row`：`source_type` 非法 |
| 533-534 | A | `validate_row`：合规红线（授权来源缺 `source_license`） |
| 539-541 | A | `validate_row`：`exam_year` 只能配 authorized/public 来源 |
| 546 | A | `validate_row`：标签数量超限 |
| 551-554 | A | `validate_row`：案例小题缺 `case_group_id` / 组号不在本文件内 |
| 576 | A | `validate_row`：单行结果 `RowOutcome` 的构造分支 |
| 642-644 | A | `create_batch`：空文件 / 超 `MAX_ROWS` |
| 870 | A | `validate_batch`：正在导入时重复校验 → 40901 |
| 883 | A | `validate_batch`：批次没有可校验行 |
| 1218 | A | `execute_batch`：状态不允许执行 → 40901 |
| 1242 | A | `execute_batch`：原始文件已不可用 |
| 1329 | A | `execute_batch`：自动发布联动（调 `publish_batch`） |
| 1363 | A | `_load_full_payloads`：原始文件已不可用 |
| 1411 | A | `_read_batch_file`：该行未存原文 → `None` |
| 1419-1420 | A | `_stored_file_size`：`OSError` → `None` |
| 1505-1510 | A | `_write_rows`：案例题分组映射（`case_group_id` → qid） |
| 1518-1520 | A | `_write_rows`：案例小题找不到父题 → 40001 |
| 1524-1525 | A | `_write_rows`：回填 `parent_id` / `root_id` |
| 1559 | A | `_write_rows`：更新模式下目标题已不存在 → 40401 |
| 1814 | A | `publish_batch`：状态不允许发布 → 40901 |
| 1818 | A | `publish_batch`：`duplicate` 动作分支 |
| 1834 | A | `publish_batch`：本批次没有可发布题目 |
| 1906 | A | `rollback_batch`：状态不允许回滚 → 40901 |
| 1985 | A | `rollback_batch`：跳过某行的 `continue` |
| 2045-2047 | A | 「回滚中再出错」的补偿路径（注入异常测，属"注入故障"不算改环境） |

### 5.2 `app/services/exam_service.py` — 73 条（A ×73）

组卷引擎的主体已被 47 条 v7 用例打得很实，剩下的全是**过滤组合**与**错误分支**。

| 行号 | 类别 | 说明 |
|---|---|---|
| 169-171 | A | `_rule_item_from_json`：**注意**，该函数整体零引用 → 见 §5.16 B 类，删掉即消这 3 条 |
| 178-183 | A | `_rule_config_of`：`rule_config` 是 `str` 时要 `json.loads`；解析失败回 `{}` |
| 231 | A | `_assert_subject_usable`：科目已停用不能用组卷 |
| 250 | A | `_rule_out`：`raw_rules` 是字符串时解析 |
| 295-299 | A | `list_paper_rules`：`status` / `rule_type` 两个过滤参数 |
| 397-399 | A | `update_paper_rule`：改 `subject_id` 前的可见性校验 |
| 400-404 | A | `update_paper_rule`：改 `name` / `type` / `duration_min` |
| 414 | A | `update_paper_rule`：改 `strategy` |
| 533-534 | A | `list_exams`：按 `exam_type` 过滤 |
| 539-540 | A | `list_exams`：按 `keyword` 模糊搜（标题 / 试卷号） |
| 675 | A | `update_exam`：试卷已删除不能编辑 |
| 692-698 | A | `update_exam`：改 `type` / `professional` / `exam_year` / `paper_no` |
| 704 | A | `update_exam`：改 `intro_html` |
| 713 | A | `update_exam`：一个字段都没传 → "没有任何要修改的字段" |
| 975 | A | `replace_exam_sections`：试卷不存在 |
| 1080-1081 | A | `weighted_sample`：候选池不够时**直接补齐**（而不是继续降档） |
| 1150-1154 | A | `_candidates`：`kp_ids` / `chapter_ids` 过滤（降档阶梯中的一档） |
| 1161-1168 | A | `_candidates`：`kp_ids` / `chapter_ids` / `year` 同档组合 |
| 1195 | A | `_resolve_rules`：`rules` 是字符串时解析 |
| 1199-1201 | A | `_resolve_rules`：试卷没有分卷 → 40001 |
| 1204 | A | `_resolve_rules`：返回规则列表的构造 |
| 1269 | A | `compose_exam`：试卷不存在 |
| 1279 | A | `compose_exam`：没有可用的抽题规则 |
| 1304-1305 | A | `compose_exam`：把分卷池按题型归组 |
| 1310-1314 | A | `compose_exam._reserve_section`：分卷名额扣减 / 扣完返回 `None` |
| 1691 | A | `_assert_no_mass_question_loss`：未达阈值直接放行 |
| 1763 / 1772 / 1780 / 1790 / 1798 / 1808 | A | `validate_exam` 的 6 条校验（分值/题型/题量等）从未被触发 |
| 1879 | A | `publish_exam`：试卷不存在 |
| 2131 | A | `_assert_exam_relations_ok`：试卷所属科目已不存在（硬约定 C 的关联校验） |
| 2266 | A | `add_exam_questions`：试卷不存在 |
| 2329 | A | `add_exam_questions`：被跳过的题目记录 |
| 2335 | A | `add_exam_questions`：跳过时的 `continue` |
| 2340-2341 | A | `add_exam_questions`：重复加题 → `skipped` + `continue` |
| 2424 | A | `remove_exam_question`：试卷不存在 |

### 5.3 `app/services/question_service.py` — 38 条（A ×38）

| 行号 | 类别 | 说明 |
|---|---|---|
| 87 | A | `_norm`：空值归一 |
| 125 | **边界 #3** | `derive_answer`：判断题 → `{"value": [bool]}` —— **单元测试已覆盖**，只是门禁看不见 |
| 134 | A | `derive_answer`：答案不是选项标号 → 40001 |
| 138 | A | `derive_answer`：多选部分给分 `partial_credit` |
| 197 | A | `scope_subject_ids`：不限范围时返回 `None` |
| 279 | A | `apply_data_scope`：给查询追加范围条件（硬约定 G 的唯一判据） |
| 339-341 | A | `_ensure_chapter`：章节不存在 / 章节与科目不匹配 |
| 462 | A | `list_questions`：按 `chapter_id` 过滤 |
| 468 | A | `list_questions`：按 `difficulty` 过滤 |
| 596 | A | `get_question_detail`：题目不存在 → 40401 |
| 793-797 | A | `create_question`：`IntegrityError` 命中唯一索引 → 40901（并发去重） |
| 862 | A | `update_question`：非法修改 → 40001 |
| 884-890 | A | `update_question`：判断题改判 / 清掉旧选项 |
| 894 | A | `update_question`：单选/多选必须有选项 |
| 899-902 | A | `update_question`：`ValueError` → 40001；判断题缺 `judge_answer` |
| 910 | A | `update_question`：改完撞上别的内容指纹 → 40901 |
| 952-953 | A | `update_question`：改 `tags` |
| 962-964 | A | `update_question`：改 `subject_id` / `chapter_id` 时的校验 |
| 1154 | A | `_assert_question_relations_ok`：章节已不存在（硬约定 C） |
| 1177 | A | 同上：知识点已不存在 |
| 1182 | A | 同上：知识点所属章节已不存在 |
| 1324 | A | `batch_soft_delete`：越权项计数提示 |
| 1486 | A | `list_chapter_tree`：挂到父节点 |
| 1539-1540 | A | `list_knowledge_points`：`keyword` 模糊搜 |
| 1680 | A | `_jsonable`：`datetime` 转 `isoformat` |

### 5.4 `app/services/rbac_service.py` — 27 条（A ×25，B ×2）

| 行号 | 类别 | 说明 |
|---|---|---|
| 66 | A | `Privileges.has_all`：子集判定（`deps.py:135` 在用它，但没有用例走到"权限不足"那一侧） |
| 136-137 | A | `get_privileges`：读权限缓存失败 → **回源数据库**（注入会抛异常的 redis） |
| 144-145 | A | `get_privileges`：写权限缓存失败 → 只记日志 |
| 151 | A | `invalidate_privileges`：`redis is None` 直接返回 |
| 154-155 | A | `invalidate_privileges`：清缓存失败 → 只记日志 |
| 159-160 | **B** | `list_roles` **整个函数零引用** —— 路由用的是 `list_roles_with_permissions`（见 §5.16） |
| 197 | A | `assign_roles`：角色不存在 → 40402 |
| 225 | A | `ensure_role_assigned`：角色不在库里（提示"请确认已执行 db/schema.sql"） |
| 228 | A | `ensure_role_assigned`：角色 id 写进本地缓存 |
| 342 | A | `build_permission_tree`：无父节点的树 |
| 358-360 | A | `_sort_tree`：递归排序 |
| 392-402 | A | `_tree_by_parent`：按父指针建树（根节点 / 子节点两条路） |

### 5.5 `app/services/auth_service.py` — 19 条（A ×19）

| 行号 | 类别 | 说明 |
|---|---|---|
| 73-75 | A | `_assert_usable`：账号被锁定 40305 / 已注销 40104 |
| 257-258 | A | `login_by_password`：已在锁定期 → 42903（带剩余秒数） |
| 268-270 | A | `login_by_password`：错够次数 → 写锁 + 清失败计数 + 42903 |
| 298-303 | A | **`login_by_sms` 主体** —— 未注册手机号直接建号（"工地扫码进来"的入口） |
| 311 | A | `login_by_sms`：写审计 |
| 324-325 | A | `login_by_sms`：`_assert_usable` + 发 token |
| 335-336 | A | `login_by_sms`：提交事务并返回 |
| 362 | A | `refresh_tokens`：凭证过期 40101 |
| 366 | A | `refresh_tokens`：账号不存在/已注销 40104 |
| 404 | A | `logout`：缺会话标识 → 40001 |

> ⚠️ **`login_by_sms` 是"整条链路零覆盖"**：服务端 19 条里占 9 条，
> 路由侧 `api/v1/auth.py:105,108` 再占 2 条。**这是本批优先级最高的一条 A** ——
> 它是一个会**自动建号**的真实入口，却没有一条用例。

### 5.6 `app/services/config_service.py` — 15 条（A ×15）

43% 缺失，看着刺眼，但其实全是**纯函数的分支**，补起来最便宜。

| 行号 | 类别 | 说明 |
|---|---|---|
| 45 | A | `get_raw`：键不存在 → `None` |
| 49-52 | A | `get_raw`：值是 JSON → 解析；解析失败 → 原样返回 |
| 65-67 | A | `get_int`：`None` → 默认值；`bool` 要单独挑出来（`True` 不能变 1） |
| 70-77 | A | `get_int`：`float` / `str` / 字符串非法 三条分支 |

### 5.7 `app/core/idgen.py` — 28 条（其中 **22 条属边界 #3**，真实待办 6 条）

| 行号 | 类别 | 说明 |
|---|---|---|
| 67-70 | A | `is_float_safe`：`OverflowError` / `TypeError` → `False`（传 `float("nan")` 或字符串） |
| 79-80 | **边界 #3** | `assert_float_safe`：断言失败抛 `ValueError` —— **单元测试已覆盖**（合并后不再缺） |
| 93-97 | **边界 #3** | `float_loss_report` 函数体 —— **单元测试已覆盖** |
| 108-111 | **边界 #3** | `decode`：把雪花 ID 拆回 ts/worker/seq |
| 134 | A | `SnowflakeGenerator.__init__`：`worker_id` 越界 |
| 149-153 | A | `_wait_next_ms`：同毫秒内自旋等待 |
| 181 / 186-187 | A | `next_id`：序列用尽 → 等下一毫秒并重置序列 |
| 204 | A | `next_ids`：`count` 为负 → `ValueError` |
| 209 | A | `next_ids`：批内唯一性 `AssertionError` 兜底 |
| 248-249 | A | `to_inet`：非法 IP → `None` |

> 也就是说：**`idgen` 的真实覆盖率是 93.8%（91/97），不是门禁报的 71%。**
> 这条完全由边界 #3 造成。

### 5.8 `app/core/deps.py` — 11 条（A ×9，B ×2）

| 行号 | 类别 | 说明 |
|---|---|---|
| 54 | A | `client_ip`：走 `X-Real-IP`（现在用例只给了 `X-Forwarded-For`） |
| 73 | A | `CurrentUser.roles`：从权限快照取角色码 |
| 113 | A | `current_user`：账号不存在/已注销 40104 |
| 134 | A | `require_permission._checker`：权限通过后返回 `me` |
| **162-163** | **B** | `rate_limit` 的 **`by="user"` 分支**：全仓 5 个调用点**全是 `by="ip"`**，这个分支永远不会执行（见 §5.16 说明） |
| **165** | **B** | `if not identity: return`：`client_ip` 在 `trust_proxy_headers=False` 且无 client 时才会返回 `None`；属防御行 |
| 175-177 | A | 限流检查自身异常 → **放行**（注入会抛异常的 redis） |
| 180 | A | 超限 → `too_many()`（通用限流器从未被测，现在只测了短信 42902） |

### 5.9 `app/services/sms_service.py` — 10 条（A ×9，C ×1）

| 行号 | 类别 | 说明 |
|---|---|---|
| 48 | A | `_incr_daily`：今日发送次数已达上限 → 42902 |
| 64-66 | A | `send_code`：Redis 异常 → 短信服务不可用（注入故障） |
| **100** | **C** | `else: await _dispatch_real_sms(...)` —— **真实短信通道**，测试恒为 `SMS_PROVIDER=mock`；它在等的不是一条用例，是**一次 staging/预发冒烟** |
| 117-119 | A | `verify_code`：Redis 异常 → 验证码服务不可用（注入故障） |
| 120-121 | A | `verify_code`：验证码已过期 40010 |
| 122-123 | A | `verify_code`：验证码不正确 40011 |

### 5.10 `app/services/user_service.py` — 7 条（A ×7）

| 行号 | 类别 | 说明 |
|---|---|---|
| 33 | A | `mask_phone`：短号不做掩码直接返回 |
| 39 | A | `_roles_map`：没有角色 → `{}` |
| 69 | A | `list_users`：按 `status` 过滤 |
| 130 | A | `assign_user_roles`：用户不存在 |
| 234 | A | `update_user_status`：非法状态值 |
| 244 | A | `update_user_status`：用户不存在 |
| 266 | A | `update_user_status`：不能禁用自己（40901）—— **注意 v3 有用例 `test_user_status_cannot_disable_self`，它之所以"缺"是边界 #1 造成的假象** |

### 5.11 `app/services/audit_service.py` — 6 条（A ×6）

| 行号 | 类别 | 说明 |
|---|---|---|
| 64-65 | A | `write_audit`：写审计失败 → 只 `warning` 不阻断（注入故障） |
| 110 | A | `_conditions`：按 `actor_id` 过滤 |
| 118 | A | `_conditions`：按 `success` 过滤 |
| 119-120 | A | `_conditions`：`start` 时间下界 |
| 123 | A | `_conditions`：`end` 时间上界 |

### 5.12 `app/core/security.py` — 5 条（A ×5）

| 行号 | 类别 | 说明 |
|---|---|---|
| 35 | A | `verify_password`：哈希为空/格式不对 → `False` |
| 39-40 | A | `verify_password`：`ValueError` / `TypeError` → `False`（防伪哈希串打崩登录） |
| 87 | A | `decode_token`：过期 → 40101 |
| 92 | A | `decode_token`：token 类型不匹配（拿 refresh 当 access）→ 40102 |

### 5.13 `app/core/errors.py` — 7 条（A ×4，C ×2）

| 行号 | 类别 | 说明 |
|---|---|---|
| 68 | A | `unavailable`：503 构造（`sms_service` 在用，但没有用例走到） |
| 93-96 | A | `_http_exception_handler`：FastAPI 自己的 `HTTPException` → 统一错误码。**打一个不存在的路径或错方法就能走到** |
| **100-101** | **C** | `_unhandled_handler`：未捕获异常兜底 500。它在等的不是用例，是**一个真的 bug** —— 要测它得先造一条"会崩的路由"，测的是脚手架不是产品 |

### 5.14 `app/schemas/*` — 14 条（A ×14）

| 行号 | 类别 | 说明 |
|---|---|---|
| `admin_question.py:67` | A | `_validate_options`：判断题不该传 `options` |
| `admin_question.py:72` | A | 选项数量不在 `MIN~MAX` 之间 |
| `admin_question.py:76-77` | A | 选项标号重复 |
| `admin_question.py:125` | A | `create`：只有判断题能传 `judge_answer` |
| `admin_question.py:169-171` | A | `update`：选项标号重复 / 数量越界 |
| `admin_exam.py:100` | A | `RuleItem`：`difficulty` 不是 `[min,max]` 或越界 |
| `admin_exam.py:110-114` | A | `RuleItem.label`：知识点数 / 章节数 / 年份的可读描述 |
| `auth.py:20` | A | `_validate_phone`：手机号格式不正确 |
| `auth.py:64` | A | `RegisterIn._password`：密码须同时含字母和数字 |
| `auth.py:85` | A | `SmsLoginIn._phone`：走同一个校验函数 |

### 5.15 其余小项

| 文件:行 | 类别 | 说明 |
|---|---|---|
| `core/config.py:83` | A | `Settings.dsn`：去掉 `+asyncpg` 给裸 asyncpg 用 |
| `core/config.py:91` | A | `_check_secret`：生产环境用默认密钥 → `ValueError` |
| `core/config.py:102` | A | `_check_sms`：`SMS_PROVIDER` 取值非法 → `ValueError` |
| `api/v1/auth.py:105` | A | 短信登录路由：调 `auth_service.login_by_sms` |
| `api/v1/auth.py:108` | A | 短信登录路由：返回 token |
| `api/v1/admin_imports.py:152` | A | 上传：文件类型/大小不合法 → 40001 |
| `api/v1/admin_imports.py:160` | A | 上传：写文件失败 → 40001 |
| `main.py:62` | **C** | 生产环境用 MOCK 短信通路的告警：要 `APP_ENV=production` 才走到，**它在等的是一次生产部署** |
| `main.py:99` | A | `GET /` → 重定向到 `/docs`（一行，打一下就有） |
| `db/base.py:57` | A | `get_redis`：进程内单例的构造分支 |
| `db/base.py:77-79` | A | `ping_redis`：Redis 不可用 → `False`（注入坏地址） |
| `db/base.py:89-91` | A | `ping_db`：数据库不可用 → `False`（注入坏 DSN） |

### 5.16 B 类逐条判断表（**6 处 / 10 条**）★

判定方式：`tools/local-verify/_find_dead.py` 扫全仓 AST 求"零引用"，再对每个名字
`grep` 一遍全仓（含 `tests/`、`tools/`）确认**只有定义那一行**；条数按行区间取
`analysis2` 的 missing **机械复算**（不再手抄 —— 初版手抄出的 7/10 矛盾就是这么来的）。

**判断原则（用户裁定 ④）**：

> 只有当代码**确实永不可能被触发**时才删，否则宁可 pragma + 说明。
> 预留类代码的判据不是"它现在被触发了吗"，而是"**它在产品/架构上有位置吗**"：
> 曾设计但没上线 → 删；该用但忘了用 → 补调用点 + 测试。

| # | 位置 | 条数 | 考古证据（`git log -S` + 字面量反查） | 判决 | 落地动作 |
|---|---|---|---|---|---|
| 1 | `db/base.py:94-98` `raw_asyncpg_connection` | 2 | 与 `cli.py:45` 的 `_raw_conn()` **逐字相同**；两者**同生于初始提交 `ac0e584`**（Batch 1-4），此后都没动过 —— 不是"后来被取代"，是**一开始就写了两份** | **保留 + 接线**<br>（该用但忘了用） | `cli.py` 改调 `app.db.base.raw_asyncpg_connection`，删本地副本 |
| 2 | `services/exam_service.py:167-171` `_rule_item_from_json` | 3 | 与 `_rule_config_of` 同批出生（`d8a15bf` Batch 7 Pass 1）；后者被后续 2 个提交继续维护，它**一次没被碰过**。唯一相关调用点 `_resolve_rules`（1195-1201）是**内联的 list 级**实现 | **删** | 备选：改造成 list 级 `_rules_from_json` 再接线 —— 那是**重构**，需要 AST + 用例证明 |
| 3 | `services/rbac_service.py:158-160` `list_roles` | 2 | 与 `list_roles_with_permissions` **同生于初始提交**；唯一相关接口 `GET /admin/rbac/roles`（`admin_rbac.py:43`）用的是后者；`docs/04` 接口清单里**没有"不带权限的角色列表"**这个需求 | **删** | — |
| 4 | `services/question_service.py:383-384` `_version_chain` | 0<br>（已 pragma） | 空函数；写下的理由是"保持模块内'版本相关逻辑集中'的可读性" —— **空函数不会让逻辑集中**，只制造"这里有东西"的错觉 | **删** | 确要占位就必须补写「为什么 + 触发条件」（硬约定 F：无标注的预留等于没写） |
| 5 | `core/deps.py:160-163` `rate_limit` 的 `by="user"` | 2 | **产品文档里有明确位置**：`docs/04 §1.4`「**用户维度 120 次/分钟**（答题 300 次/分钟）｜Redis 令牌桶」；`docs/05:330`「短信轰炸 → 图形验证码前置 + **手机号/IP 双维度限流**」。而实现里 **5 个调用点全是 `by="ip"`** | **保留 + 补调用点 + 测试**<br>（该用但忘了用） | 见下方 ⚠️ |
| 6 | `core/deps.py:165` `if not identity: return` | 1 | 可达条件：无 `X-Forwarded-For`/`X-Real-IP` 且 `request.client is None`。**它有正确性语义** —— identity 为空时 key 会变成 `rl:name::window`，**所有人共享同一个桶**（谁先打满谁封全站） | **改判 A**<br>（补用例，不删也不 pragma） | 手工构造**不带 `client` 的 ASGI scope**，直接调 `rate_limit(...)(request, redis)` |

**合计 10 条** —— 判决分布：**删 5 条**（#2 三 + #3 两）／**接上 4 条**（#1 两 + #5 两）／
**回归 A 1 条**（#6）。加 `_version_chain` 的 2 条（已被 pragma 排除，不进 579）。

#### #2 为什么是"删"、而 #1 / #5 是"接上"

三条都是"没人调用的代码"，但**签名的可用性**决定了处理方式：

| | 有位置吗 | 签名能用吗 | 判决 |
|---|---|---|---|
| #1 `raw_asyncpg_connection` | 有（分层铁律：数据访问归 `db/`） | **能** —— 与副本逐字相同，换过去零风险 | 接上，删副本 |
| #5 `by="user"` | 有（`docs/04 §1.4` 用户维度限流） | **能** —— 挂到已鉴权端点上即可 | 接上 + 补测试 |
| #2 `_rule_item_from_json` | 有（"统一 JSONB → RuleItem"） | **不能** —— 它是 **per-item** 的，而真实调用点要 **list 级** | 删（留着会诱导写错） |

⚠️ #2 那条"接上去就会写错"值得单独说：如果照 docstring 的意图写成
`[_rule_item_from_json(x) for x in rule_row["rules"]]`，**当 `rules` 是 JSON 字符串时，
`for x in raw` 会按字符迭代** —— 得到一堆单字符再 `model_validate`，报的错还离现场很远。
**一个签名与需求不匹配的 helper，比没有这个 helper 更危险。**

#### ⚠️ #5 有两个细节需要你先定（我没有替你决定）

**(a) 挂在哪些端点？** auth 的 5 个端点都是**未鉴权**的，用 `by="ip"` 是对的 ——
「用户维度」应该挂**已鉴权的重接口**。我建议先挂这两个（都已在库里核实过路径）：

- `POST /admin/imports` —— 上传解析（背后是 618 语句的 `import_service`）
- `POST /admin/paper-rules/preview` —— 组卷试算 dry-run（会跑真实抽题）

**(b) 限值定多少？** 设计写 120 次/分钟，但**先要确认现有用例会不会打满**：
全量用例里同一个 token 在一分钟内的请求数若超过 120，用例会开始吃 429 而变红。
落地顺序：**先接上 → 跑一次 `run-smoke.ps1` → 若红，改用独立限流 key（不同 `name`）或放大窗口**。
⚠️ **不要为了过用例把限值调成没有意义的大数字** —— 那会让这条接线变成装饰。

> **删 B 类的意义不是"抬数字"**：`list_roles` 是那条 N+1 的诱饵；
> `_rule_item_from_json` 的签名接上去就会写错。它们留在库里，
> 下一个人**很可能挑错那个用** —— 那才是真正的成本。

### 5.17 `app/cli.py` — 边界 #2 的处置（**已执行：插桩 + combine**）

**用户裁定**：插桩 + `combine`，**不 omit** —— 受益的不止 `cli.py`，
未来 CLI / worker / 定时任务都走同一条路。

**改法**（只改 `tools/local-verify/run-smoke.ps1`，**不动 `cli.py` 结构**）：
三个进程各自插桩，最后合并。数据文件命名 `.coverage.api` / `.coverage.cli` /
`.coverage.tests`，合并结果 `.coverage`：

| 进程 | 采到什么 | 怎么采 |
|---|---|---|
| API | HTTP 用例打到的业务代码（大头） | `serve_fake_redis.py --coverage --cov-data-file .coverage.api` |
| CLI | `seed-rbac` / `seed-admin` 两条子命令的整条路径 | 调用点套 `coverage run --append --data-file .coverage.cli` |
| pytest | 单元测试**直接调 app 代码**的那部分 | pytest 套 `coverage run --data-file .coverage.tests` |

三条约束（用户给的，都已实测）：
1. **任一份缺失或为空 → 报错，绝不静默合并**（否则"某条路径没跑到、覆盖率却看着正常"）
2. **合并要确定性**：两次独立运行 → 同一个结果
3. 采集前清场（否则 `--append` 会把上一轮数据攒进来，数字虚高且不可复现）

**实测（从零库，两次独立运行）**：

```
128 passed, 1 skipped          exit 0        （两次都是）
三份数据各 53,248 bytes  →   Combined 3 files
TOTAL   4299 语句 / 462 未覆盖  →  89.25%     （合并前 4299 / 571 → 86.72%）
确定性：两次运行的合并文件 sha256 逐字节相同
        1b3545be24146c71e02f75524400899f15be739a481839572def2dbd587b2842
```

**反向测试（守卫本身也要被验收，硬约定 H）**：临时在合并前删掉 `.coverage.tests`，
脚本**显式失败**且**不出报告**（避免把不完整数据当结论）：

```
[local-verify] 失败：覆盖率数据缺失或为空：.coverage.tests（pytest 进程） —— 拒绝静默合并。
[wrapper] run-smoke exit code = 1
```

**增量清单：缺口 571 → 462（−109）** —— 这些行**本来就在执行**，只是不在账上：

| 文件 | Δ | 说明 |
|---|---|---|
| `cli.py` | **−80** | 模块级 import + `seed-rbac` / `seed-admin` 的整条路径 |
| `core/idgen.py` | **−22** | `test_idgen.py` 直接调用（边界 #3） |
| `db/base.py` | −2 | `raw_asyncpg_connection` 的 `import asyncpg` + `connect` —— **② 的接线在这里才显形** |
| `services/question_service.py` | −2 | `derive_answer` 的判断题 / 多选分支（边界 #3） |
| `core/config.py` | −1 | `Settings.dsn` |
| `core/deps.py` | −1 | **165（空 identity 守卫）** —— ② 那条用例（边界 #3） |
| `schemas/admin_exam.py` | −1 | `RuleItem._check_difficulty` 的校验分支 |

⚠️ **`cli.py` 仍有 108 条未覆盖** —— 那是 `init-db` / `wait-db` / `wait-redis` /
`seed-questions` 这些**冒烟管道没调用的子命令**。它们是**真实缺口**（该测），
不是采集问题 —— 判据很简单：**这次它们确实没被执行过。**

> 边界 #2 处置完毕。**step ④ 抬门槛的依据就是本节**：89.25% → 临时值 85。

## 6. 门槛怎么定（套用你的规则）

你的规则：`X ≥ 71 → fail_under = X − 1`；`X ≥ 75 → fail_under = 70（保守）`。

| 前提 | X | 套用结果 |
|---|---|---|
| **只修边界 #1**（今天的状态） | 86.55% | ≥75 → **70** |
| 边界 #1 + #3（`combine` pytest 进程） | 87.11% | ≥75 → **70** |
| 边界 #1 + #3 + #2 方案②（`omit cli.py`） | 91.13% | ≥75 → **70** |

⚠️ **这里有一个你的规则没覆盖到的情况，需要你补裁定**：
三条路算出来的 X 是 86.55 / 87.11 / 91.13，**按"≥75 → 70"三条都落到 70**，
而现状已经是 65 —— 也就是**门槛只涨 5 个点，但实际值涨了 21 个点**。
"留 1 个点余量"的设计意图（让正常开发不撞红）在 70 上确实满足了，
但**棘轮几乎没动**，下次再有类似假数字也拦不住。

我的建议是**分两步走**，而不是一次定死：

1. **本批把 `fail_under` 提到 85**（贴着实测 86.55 − 1.55）——
   先让棘轮真的动起来，把"假数字"这条路堵死；
2. **补测批做完再按 `X − 1` 重定一次**，并把**新的触发条件**写进 `.coveragerc`
   （`实测 ≥ 90% → 抬到 89`）。

**✅ 用户 2026-09-21 裁定：`fail_under = 85`**（临时值）。理由是上面那条：
旧规则套在修正后的基准上会得出"几乎不动"的结论，违背了棘轮自己的目的。

**下一档触发条件（已写进 `.coveragerc`）**：

```
触发：实测总覆盖率 ≥ 90%   →   动作：改成「实测值 − 1」并重写那一段
```

⚠️ 85 是**临时值**不是新基线 —— 它的作用是**先把"假数字"这条路堵死**
（按 86.55% 算，旧门槛 65 意味着"掉 21 个点都还是绿的"）。
补测批完成后按 `X − 1` 重定，**不要让它变成第二个"从来没动过的棘轮"**。

---

## 7. 本批改了什么 / 没改什么

**改了（都是采集与诊断，不碰产品逻辑）**

| 文件 | 改动 | 为什么必须现在改 |
|---|---|---|
| `.coveragerc` | 新增 `[run] concurrency = greenlet` + 写明根因与实测证据 | 不改它，本清单就是一份**假清单**（74% 的条目是幻影） |
| `.coveragerc` | 更正 `[report]` 里已作废的实测数字（65.64% / services 49.6% / 1153） | 陈旧引用（你的交付习惯第 3 条） |
| `tools/local-verify/run-smoke.ps1` | 前置依赖检查加入 `greenlet` | `concurrency=greenlet` **依赖** greenlet 包；它是 `sqlalchemy[asyncio]` 的传递依赖，**"本地碰巧装了"≠ CI 装得到**（硬约定 H） |
| `tools/local-verify/cov-gaps.py` | **新增**常驻诊断工具 | 本清单就是它生成的；以后每批可重跑 |
| `tools/local-verify/_find_dead.py` | **新增**（探针，待收编） | B 类的"零引用"判定必须机械化 |
| `tools/local-verify/_probe_greenlet.py` | **新增**（探针） | 边界 #1 的判别性实验，误判时才有的关键证据 |

**没改**

- `apps/api/app/**`、`apps/admin/**` —— **一行产品代码都没动**
- `tools/` / `db/seed` 的 ruff 作用域扩展（你说过单独一批）
- pre-commit hook（你的裁定，理由保留在 `docs/17`）

**✅ 四项裁定（2026-09-21）与执行顺序**

| # | 事项 | 裁定 | 状态 |
|---|---|---|---|
| ① | 边界 #2 `cli.py` | **插桩 + `combine`，不 omit** —— 受益的不止 `cli.py`，未来 CLI / worker / 定时任务都走同一条路 | 待做 |
| ② | 边界 #3 的 24 条 | **纳入 `combine`** | 待做 |
| ③ | `fail_under` | **85**（临时值，下一档触发条件写进 `.coveragerc`，见 §6） | 待做 |
| ④ | B 类 10 条 | 按 §5.16 逐条判决：**删 5 / 接上 4 / 改判 A 1** | 表已出，**等确认再动代码** |

**顺序**：① 确认 §5.16 表 → ② `cli.py` 插桩 + `combine` → ③ 抬 `fail_under` 到 85
→ ④ 补测 `login_by_sms` 链路（最高优先的 A）。

**本批不做**（用户明确排除）：`tools/` 与 `db/seed` 的 lint 扩展（6 条既有问题，单独一批）；
pre-commit hook（门禁已在 CI 全拦截，本地再挂一层只会引入"钩子没装成功就静默跳过"的新病）。

---

## 8. 复查清单（第二步开工前）

- [ ] **`.coverage` 里的数字现在合成了三份来源，别混用** ——
      写文档/汇报时明确写"哪个口径"，本项目已经因为口径混用吃过大亏
- [ ] 补 A 类时**先确认它不是因为边界 #1 才显得"缺失"** ——
      凡是"用例明明测了却报缺失"的，先按 §1.1 的方法验一遍
- [ ] 补 A 类要**构造反例**，不是把正常路径再写一遍（你的原话）
- [ ] 每条新增用例做**变异验证**：把对应的 `raise` 注释掉，看用例是否真变红
- [ ] 删 B 类后**重跑一次 `coverage report`**，确认"删掉的代码不需要测试"这半句话成立
- [ ] 数字收尾：`docs/17`、`README`、`apps/admin/README` 里的覆盖率数字同步
- [ ] **硬约定 J**：测量工具自己验过吗？（判据：**打掉被测量对象，测量值应该变**）
- [ ] **硬约定 K**：`git push` **没接管道**吧？（`> 文件 2>&1` 再读，不要 `| tail`）
- [ ] **硬约定 L**：子进程跑的业务代码，**在子进程插桩**了吗？（不要 `omit`）

---

## 9. ⑤a 执行结果（2026-09-22）：短信登录链路 + 服务层错误分支

**范围**（用户裁定）：`login_by_sms` 整条链路 + 5 类反例。
新增 **14 条用例**、**生产代码只加注释**（零行为改动）。

### 9.1 数字

    用例       128 passed, 1 skipped   →  142 passed, 1 skipped        （+14）
    未覆盖     462 → 435                                              （−27）
    覆盖率     89.25% → 89.88%

| 文件 | 前 | 后 | 变化 |
|---|---|---|---|
| `services/auth_service.py` | 19 | **8** | −11 |
| `services/sms_service.py` | 10 | **0** | −10 |
| `api/v1/auth.py` | 2 | **0** | −2 |
| `schemas/auth.py` | 3 | **0** | −3 |

`auth_service` 剩下的 8 条**不在 ⑤a 范围**（是另外三条流）：密码登录锁定 5
（`257/258/268/269/270`）、refresh 2（`362/366`）、logout 1（`404`）→ 留给下一批。

### 9.2 三个"HTTP 打不到"的硬约束（决定了必须分两半）

1. **同一手机号 60 秒冷却**（`sms_service._send_code` 的 `set(..., nx=True)`）⇒
   "同一手机号发两次码"单次运行做不到 → 「已注册用户再次登录」「锁定/注销后登录」
   这两类**需要第二次发码**的场景，HTTP 覆盖不了。
2. **每日上限**：手机号维度 10 次 + 60 秒冷却 → 不可能；IP 维度 20 次
   而**路由层本身就是 20 次/60s** → 先撞路由限流（`42901`），走不到服务层的日上限。
3. **`_dispatch_real_sms`** 需要 `SMS_PROVIDER != mock` 的 **API 进程** ——
   而 API 由管道固定用 mock 起，运行中改不了。

⇒ 拆成 **HTTP 半**（5 条：1 对照 + 4 反例）与 **进程内服务层半**（9 条）。
⚠️ 进程内这一类**现在才算数** —— **step ③ 给 pytest 进程插桩之后**才被记上账
（硬约定 L 的直接回报）。

### 9.3 变异验证：**14 / 14 全部变红**

| 编号 | 打掉了什么 | 对应用例 |
|---|---|---|
| M1 | 日上限不再拦 | `test_send_code_enforces_daily_limit` |
| M2 | send 的 Redis 故障不再转 503 | `test_send_code_reports_503_when_redis_is_down` |
| M3 | verify 的 Redis 故障不再转 503 | `test_verify_code_reports_503_when_redis_is_down` |
| M4 | 真实通道缺失时不再报错 | `test_send_code_with_unconfigured_real_provider_is_503` |
| M5 | `status='locked'` 不再拦 | `test_locked_account_is_refused_on_sms_login` |
| M6 | `status='deleted'` 不再拦 | `test_account_marked_deleted_is_refused_on_sms_login` |
| M7 | 已注册也强行新建号 | `test_existing_phone_logs_into_the_same_account` |
| M8 | 注销后仍复用旧号（不释放） | `test_deleted_account_releases_the_phone` |
| M9 | 手机号不再校验 | `test_auth_schemas_reject_bad_phone_and_weak_password` |
| M10 | 没发过码也能进 | `test_code_that_was_never_sent_is_rejected` |
| M11 | 错码也能进 | `test_wrong_code_is_rejected_and_not_consumed` |
| M12 | 码不再一次性消费（可重放） | `test_code_is_single_use` |
| M13 | 码不再与手机号绑定 | `test_code_is_bound_to_phone` |
| M14 | 不再自动建号 | `test_sms_login_happy_path_auto_registers` |

⚠️ **第一版 harness 有三个 bug，每一个都会导致"假绿"，必须记下来**：

1. **基线检查放在施加变异之后** —— 那次"基线红"正是变异自己造成的；
2. 基线失败时 `continue`，**跳过了还原** → 后面所有用例都带着上一个变异跑；
3. **施加变异后没重启 API** → HTTP 那一半的变异根本没生效
   （第一版"5 个里有 2 个仍绿"就是这么来的，**不是用例的毛病**）。

**与坑 51 / 55 同族：验证脚本本身也要被验证**（硬约定 H）——
它报的是"绿"，而假绿比报错危险。

### 9.4 顺带查清的两条契约

- **`/auth/me` 对本人返回明文手机号**（脱敏只在"给别人看的列表"）——
  我第一版把它读成"永不返回明文"，被自己的用例当场纠正；现已**固定成契约**，
  以后谁要改成对本人也脱敏，必须显式面对这条断言。
- **注销（`is_deleted=true`）= 释放手机号**：同号再登录会**建新号**（`user_id` 变化）。
  已写进 `auth_service` 两处注释 + `docs/04` §2.1（含"引入试用/权益前重新评估"的待办）。

---

## 10. ⑤d 执行结果（2026-09-23）：`auth_service` 剩余 8 条 → **清零**

### 10.1 数字（`run-smoke.ps1`，从零库）

```
221 passed, 1 skipped        exit 0        （⑤c-3/4 后：217 passed / 1 skipped）
TOTAL  4291 语句 / 318 未覆盖 → 92.59%      （⑤c-3/4 后：4293 / 327 → 92.38%）
```

| 文件 | 前 | 后 |
|---|---|---|
| `services/auth_service.py` | 19 → 8（⑤a 之后） | **0** ← 清零 |
| `services/import_service.py` | 1 | **0**（pragma 落地） |
| 语句总数 | 4293 | **4291**（−2：pragma 排除） |

缺口 **327 → 318（−9）** = 8 条被新用例吃掉 + 1 条被 pragma 排除。

### 10.2 新增 4 条用例，每条固定一个**契约**

| # | 契约 | 覆盖 |
|---|---|---|
| ① | 连续失败达阈值 → 建锁；**锁定期间密码正确也拒绝** | 263 / 264 / 274 / 275 / 276 |
| ② | 会话在**库里**过期 → 40101（JWT 自己仍有效） | 374 |
| ③ | 刷新时账号已注销 → 40104 | 377 / 378 |
| ④ | access token 无 `sid` → 单设备登出 40001 | 415 / 416 |

四条写法都有可复用之处：

- ①把「**顺序**」当契约压：锁检查必须排在密码校验**之前** —— 否则每次错密码都白烧
  一次 ~250ms 的 bcrypt（既浪费又是攻击面）。用例断言"锁定期间**正确密码也被拒**"，
  顺序反了这条就红。
- ②只改**库里的 `user_sessions.expires_at`**、不动 JWT —— 否则命中的是 `decode_token`
  阶段的 exp 校验，验不到"**服务端能主动作废**"这个真正的契约。
- ③改过库 → `try/finally` 还原，**finally 里直接执行、不判断前面成没成**。
- ④的 token 是**手搓的**（`jwt.encode`，不调 `security._encode`）—— 绑私有函数会在重命名时误红。
  并附**正向对照**：同一 token + `all_devices=true` 必须成功，排除"token 压根不被接受"的假绿。

### 10.3 变异验证：**5 / 5 全部捕获**

| 变异 | 打掉的判定 | 结果 |
|---|---|---|
| M1 | `if await redis.exists(lock_key):` → `False` | ✅ 红（第 6 次用**正确密码**却登录成功） |
| M2 | `if fails >= settings.login_max_failures:` → `False` | ✅ 红（第 5 次仍返回 40103） |
| M3 | `if session.expires_at <= now:` → `False` | ✅ 红（`assert 0 == 40101`） |
| M4 | `if user is None:`（refresh 侧）→ `False` | ✅ 红（`assert 50001 == 40104`：None 上抛 AttributeError） |
| M5 | `if session_id is None:` → `False` | ✅ 红（`assert 0 == 40001`） |

还原后基线复绿。

**另加一次行级核对**（harness 带 `--coverage` 只跑这一个文件）：8 条目标缺口**全部命中**，
且 5 个**守卫行**（262 / 273 / 373 / 377 / 415）也命中 —— 说明压的是**判定本身**，不只是文案。

### 10.4 ⚠️ harness 又踩了三个坑（共同点：**都会静默误判**）

| # | 现象 | 根因 | 教训 |
|---|---|---|---|
| 1 | ⑤c-3 报"13 个存活" | stop 用的哨兵路径 ≠ 被启进程用的那份 → 旧进程占着 8123 | **就绪判据要能回答"这是不是我起的那一个"**（已升格为验证套路） |
| 2 | 自检拦下"M4 锚点出现 2 次" | `if user is None:` 在 `login_by_sms` 里也有一处 | **替换前先断言锚点唯一** —— 改错地方**不会报错** |
| 3 | 基线判"红"，而**一条用例都没跑** | 把**文件名**当用例名拼成 `file.py::file.py` → pytest not found、退出码 4 | **绿/红判据本身也要先被验证**；失败必须打印 pytest 尾部，不靠猜 |

坑 3 留下的"指纹"值得记住：覆盖率文件**几乎是空的**（缺行列表里全是别的用例本该覆盖的行）。
如果当时直接"照缺行去补测"，就会对着一份**根本没跑**的结果干活。

### 10.5 棘轮：本批**不动**（新规则第一次走"不动"分支）

`.coveragerc` 的动态规则是「**实测 ≥ 门槛 + 2** → 门槛 = 实测 − 1」：

```
实测 92.59 / 门槛 90.75  →  92.59 ≥ 92.75 ❌  →  **不动**（余量 1.84pp）
```

旧的固定规则（"实测 ≥ 90%"）会在这里抬到 91.59，把余量压到 1.0pp；
**动态规则自己挡住了** —— 这正是它相对固定阈值的价值。

---

## 11. ⑤b 执行结果（2026-09-23）：`cli.py` 六个子命令 → **清零**

### 11.1 数字

```
221 → 252 passed, 1 skipped            exit 0
4290 语句 / 318 → 210 未覆盖            92.59% → 95.10%（门槛 90.75，余量 4.35pp）
app/cli.py：188 → 187 语句 / 108 → 0    排除 3 条（两处 `__main__` + 一条不可达 return）
```

缺口 **318 → 210（−108）**：107 条被新用例吃掉 + 1 条 pragma。

### 11.2 ★ 为什么测试要**在进程内**调 `cli.main([...])`

`run-smoke.ps1` / CI 都用 `coverage run -m pytest` 包住 pytest，而**子进程不在插桩范围内**
（没配 `COVERAGE_PROCESS_START`）。所以用 `subprocess` 起 `python -m app.cli` 的后果是
**用例全绿、`cli.py` 一行覆盖率都不涨** —— 一个"看起来补了、其实没补"的假象。

`run-smoke.ps1` 里那两次 `coverage run ... -m app.cli seed-rbac/seed-admin` 之所以有效，
正是因为它们**在 CLI 自己的进程里**插桩（硬约定 L：跨进程执行 = 跨进程测量）。

### 11.3 六个子命令 + 运维必须正确的分支（31 条用例）

| 子命令 | 固定的契约 |
|---|---|
| `wait-db` | 可连 → 0；不可达 → **超时后 1**（不能永远挂着），并断言"确实重试过" |
| `wait-redis` | 同上；用**最小 Redis 协议桩**造出"就绪 → 0"这条真实环境到不了的分支（否则"不可达 → 1"没有正向对照） |
| `init-db` | 已建 → 跳过（0）；**缺 schema.sql → 跳过（0）**；**空库 → 真的建表**；SQL 坏 → **1** |
| `seed-rbac` | 切片标记缺失 / 空切片 / SQL 非法 → **一律 1 且不静默**；真 schema 上重放**幂等** |
| `seed-admin` | 缺手机号 → 0；口令太短 → **1**；无 super_admin 角色 → **1**；**超管不存在 → 建出来** |
| `seed-questions` | 缺文件 → 0；**题库非空 → 跳过且条数不变**；空科目表 → **1**；导入失败 → **1** |
| 入口 | 非法子命令 → 退出码 **2**；六个子命令**各自路由到自己的处理函数**（用哨兵值 `7` 证明返回码是"带出来"的，不是恰好也是 0） |

### 11.4 ★ 补测抓到的两处"两个环境都没走过"的路

1. **`init-db` 的"从零建库"** —— `run-smoke.ps1` 与 CI 都是**先用 psql 建表**再调 CLI，
   于是永远走"已存在 → 跳过"。而这条路正是 `docker-entrypoint.sh` 首次起容器时依赖的。
   用**临时库**（`CREATE DATABASE` … 用完 `DROP ... WITH (FORCE)`）造出来；
   顺带验证了 `_init_db` 用 **asyncpg 一次性执行整份 schema.sql** 可行
   （若 schema.sql 里混了 psql 元命令，这条会直接红 —— 那正是"首次部署起不来"的缺陷）。
2. **`seed-admin` 的"超管不存在 → 建号"** —— 本地库早有超管，一直走 `exists` 分支。
   用随机手机号跑，`finally` 里按外键顺序删干净（否则排在后面的 `test_admin_*`
   会在用户列表里看到多余的超管）。

### 11.5 变异验证：**12 / 12 全部捕获**

打掉的判定：解析器候选循环 / `init-db` 缺文件 / RBAC 切片标记 / 空切片 / 空手机号 /
口令长度 / 角色缺失 / 空科目表 / 题库非空 / redis 重试循环 / wait-db 成功返回码 / main 分派表。
还原后基线复绿；行级核对 `cli.py` **187 语句 / 0 未覆盖**。

### 11.6 ⚠️ 三处环境坑（同一家族：**都会静默误判**）

| 现象 | 根因 | 处置 |
|---|---|---|
| 测试全绿，但 pytest **会话结束时 `SystemExit(1)`**（更糟的一次挂了 23 分钟） | 本机沙箱给 `shutil.rmtree` 装了"批量删除守卫"，pytest 清理**自己的临时目录**时命中，非交互环境下变成退出码 1 / 卡住等批准 | **不用 `tmp_path`**：自己建目录、只写单文件、不删目录 |
| harness 的**整文件基线**判"红"，而一条用例都没跑 | 把**文件名**当用例名拼成 `file.py::file.py` → pytest not found、退出码 4。指纹：覆盖率文件几乎为空 | 绿/红判据本身也要先被验证；失败必须打印 pytest 尾部 |
| `run-smoke` 报"**覆盖率未达 fail_under**"，实际是 combine/report 被守卫拦了 | 沙箱守卫的退出码被当成了 report 的退出码 | 实测同一份 `.coverage` 手动 report：**4290/210 → 95%**、rc=0（门槛本就过） |

> 第 3 条值得单列：**"失败信息指向的地方"与"真正的失败点"可以不是一回事** ——
> 我差点去查 fail_under 的计算，而真因是沙箱守卫。

### 11.7 棘轮：本批**触发**

```
实测 95.10 / 门槛 90.75  →  95.10 ≥ 92.75 ✅  →  按规则应抬到 94.10
```

（与 ⑤d 那次"92.59 < 92.75 → 不动"正好构成一对：**动态规则既会挡住过早抬，也会及时抬**。）

### 11.8 剩余缺口（16 个文件 / 210 条）

```
exam_service.py   70 / 714      question_service.py  38 / 433
rbac_service.py   27 / 156      config_service.py    15 /  35
schemas/admin_question.py 7     core/deps.py 7     services/user_service.py 7
db/base.py 7     core/idgen.py 6     core/errors.py 6   …
```

**都不是"真入口 / 真功能"**（防御性分支、辅助方法、工程命令）—— 与"补测批暂停判据"的
第 ② 条相符。
