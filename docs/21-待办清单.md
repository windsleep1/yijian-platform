# 21 · 待办清单（**唯一索引**）

> 建立日期：2026-09-25 ｜ 来源：用户 2026-09-25 要求（"盘点现有『以后』，逐条处理"）
>
> **本文件是"待办"的唯一权威索引。** 每条待办在这里必须有三样东西：**编号 / 触发条件 / 检查点**。
> 详细设计分散在各自文档（`docs/14` §7、`docs/15`、`docs/20` 等），本文件负责"**它还没被忘掉**"。
> 两者的分工：**别处写"怎么做"，这里写"什么时候必须想起来"**。

---

## 1. 规则：一条"待办"必须满足三个条件（2026-09-25 定，硬约定 P）

| # | 条件 | 反例（不达标） |
|---|---|---|
| 1 | **有编号** —— 每条在文档里有唯一 ID | "记得优化一下" |
| 2 | **有触发条件** —— "**X 发生时做**"，不是"重要时做" | "以后有空再说"、"后续优化" |
| 3 | **有检查点** —— 每批收尾扫一遍清单，看有没有触发 | 写了但没人再看 → 退化成"一个数字" |

> **不满足三条件的"待办"，要么现在做，要么明确写"不做 + 理由"。**
> **"以后想起来"不构成待办 —— 它是逃避。**

**为什么必须有检查点**：前两条只保证"写得清楚"，**没有任何交付流程会因为"当时说过"而停下来**
（同 `.coveragerc` 里"抬升规则"那条：门槛一旦写死就没人回头看，棘轮退化成数字）。
所以第 3 条的落点是**机械动作**：`apps/admin/docs/B端联调坑.md` 附录「一批交付收尾的固定动作」**§8**。

**触发条件的判据本身也要验**（硬约定 J）：一条触发条件若**当天就已满足**，它不是"以后要做"，
而是"现在做"或"假条件" —— 本项目已抓到一次（**BL-04**，用户原话"角色数 ≥ 3"，而种子里已有 7 个角色）。
⇒ 写触发条件时**必须回头查一次"它现在是不是已经成立"**。

---

## 2. 清单（当前有效）

状态约定：🔵 进行中 ｜ 🟡 待执行（触发条件里的**事件已发生**，只是还没做）｜ ⚪ 未触发 ｜ ⛔ 不做

| ID | 内容 | 触发条件（可判定） | 状态 | 权威细节 |
|---|---|---|---|---|
| **BL-01a** | `questions` 审核四件套（`reviewed_by` / `reviewed_at` / **理由** + 独立接口 `POST /admin/questions/{id}/review`）+ 送审 `POST .../submit`，并写 `content_change_logs` | **本批**（用户 2026-09-25 指定，排在 C 端之前） | ✅ **后端已落地**（16 用例 / 变异 5 捕获）；**前端待做** | `docs/15` §7–§13 · **§15 执行结果** |
| **BL-01b** | `comments` 审核四件套（`audited_by` / `audited_at` + 审核意见） | **C 端评论区上线时** | ⚪ 未触发 | `docs/15` §4 序 2 |
| **BL-01c** | 主观题人工批改（`exam_attempt_items.reviewer_id` / `review_comment` / `reviewed_at`）+ `exam_attempts.scoring` 推进 | **C 端作答 / 批改批次**；⚠️ **必须**与 `unpublish` 的"有 N 人在考"校验**同批**（拆开会出现"先上线了下线功能、再补校验"的窗口期） | ⚪ 未触发 | `docs/15` §4 序 3 · `docs/14` §7.3 |
| **BL-02** | 统计看板 **S3**（造数段 → 真实数据段） | **题库里有真实作答数据时**（`exam_attempts` 出现第一条**非种子**记录） | ⚪ 未触发 | `docs/20` §8 · §9 |
| **BL-03** | 内容运营 / 课程 / 支付链路 | — | ⛔ **不做**（理由见 §3.1） | `docs/14` §7.5 / §7.6 |
| **BL-04** | **显式 per-role 落地页配置**（`roles.landing_module` 或前端覆盖表），取代"权限过滤后第一个命中" | **再次为了某个角色的落地页去修改 `MODULE_ENTRIES` 的顺序时**（已发生过 1 次：Batch 8 把 `operator` 顺带改成 `/dashboard`） | ⚪ 未触发（1/2） | `permission.ts` 注释 · `docs/20` §14.6 |
| **BL-05** | 部署前在**不受沙箱限制的环境**跑一次 `npm run build`（前端） | **任何一次部署 / 上线前** | ⚪ 未触发 | `docs/06` §9.1 专条 · 坑 60 |
| **BL-06** | `--junitxml` 跨宿主编码（硬约定 I 的落点） | — | ⛔ **不做**（理由见 §3.2） | 坑 55 · 硬约定 I |
| **BL-07** | 把 **CI 的覆盖率数字**与本地对比一次（用户 2026-09-25 ③） | **下一次含门禁的 CI run 跑完时**（当前**本地领先远端、未推**，CI 还看不到这批代码） | 🟡 待执行（**阻塞在推送**） | §4 |
| **BL-08** | **审核权责独立**：独立权限码（`question:review`）+ "审核人 ≠ 出题人"校验 | **第 8 个角色被加入时**，或 **`role_permissions` 不再按 module 整包发放时** | ⚪ 未触发（当前 7 个角色） | `docs/15` §8.3 |
| **BL-09** | **移除 `PUT /admin/questions/{id}` 的 `status` 入参**（发布 / 驳回 / 送审已各有独立入口），并顺带把前端 `QuestionForm` 的「状态下拉框」改成**只读展示 + 动作按钮** | **下一次改 `update_question` / `QuestionUpdateIn` 时**（自然的去处：你已经在那个文件里） | 🔵 **前端已做完**（下拉框已只读、`diffDraft` 不再带 status、`QuestionUpdateIn` 已 `Omit<...,"status">`）；**后端入参移除待做** | `docs/15` §12.1 · §4.2 |
| **BL-10** | **把 `ruff format --check` 的范围从 `apps/api` 扩到全仓**（需先全仓格式化 29 个文件） | **「风格统一第二步」立项时**（`ruff.toml` 里已经写明这一步要独立一个 commit） | ⚪ 未触发（存量 29 文件 > 20，见 §4.3） | `.github/workflows/ci.yml` 注释 · `ruff.toml` |
| **BL-11** | `status='archived'`（业务归档）**没有任何 UI 入口**（BL-09 前端的副作用） | **BL-09 后半段移除 `PUT` 的 `status` 时，必须三选一**（删状态 / 补独立入口 / 归入软删除）——**不许"再看 / 暂缓 / 待定"** | ⛔ **不做**（本次；触发时刻必须给结论，见 §3.3） | §3.3 · `docs/14` §1 |
| **BL-12** | **把 `next build` 纳入 CI**（前端 job 最后一步）。它同时闭合两件事：BL-05 的"部署前手工跑 build"，以及"**跨包 TS 能不能被 Next 打出来**"——**后者只有 build 会暴露** | **决定抽 `packages/api-core` 之前**（即 `docs/22` §9.2.3 选 **(A)** 时）；若选 (B) 则不触发，但 **BL-05 仍然挂着** | ⚪ 未触发（等 §9.2.3 的 A/B 裁定） | `docs/22` §9.2.3 · 坑 60 · BL-05 |
| **BL-13** | **登出 / 撤销会话对 access token 不即时生效**：`current_user`（`deps.py:96-118`）**从不读 `user_sessions` 行**，只解 token + 查权限 ⇒ **登出后 token 仍有效到自然过期** | **C 端实现登出时**（届时用户会期待"登出立即失效"）；⚠️ `/admin/*` 已由 `docs/22` §6.5 顺带修掉，**C 端仍未修** | ⚪ 未触发 | `docs/22` §6.5.4 · `docs/23` #11 |
| **BL-14** | **`daily`（每日一练）的口径统一**：① `docs/04` 自己不一致（既是 `sessions` 的 `mode`，又是独立端点 `/practice/daily`）；② "**每日唯一**"**DB 无任何唯一约束**（`practice_sessions` 只有普通索引 `idx_practice_user_mode`） | **做每日一练（`mode='daily'`）时** —— 届时**必须定**：是 `mode` 还是独立端点，以及"唯一"用**部分唯一索引**还是代码保证 | ⚪ 未触发 | `docs/23` #9 · #10 |

> ⚠️ BL-04 的触发条件**已按上表规则收窄过**：用户原话是"当角色数 ≥ 3 时"，
> 而种子里**已有 7 个角色** → 按字面**当天就满足**，属于"假条件"。已改写为
> "**又有人想调 `MODULE_ENTRIES` 的顺序**"（那才是这张表扛不住的信号）。理由同 §1 末段。

---

## 3. 明确**不做**的三条（不是"以后"，是"已裁定不做"）

### 3.1 BL-03 · 内容运营 / 课程 / 支付

**理由：边际价值低。** 本平台的价值集中在**题库 → 组卷 → 模考**这条主链上；
课程与支付要真做，必须同时拉起来**支付合规、退款、对账、发票、课时管理**一整条链，
成本远高于它带来的收益。

**重启条件（不是"永远不做"）**：出现**明确的付费需求**，或 **B 端客户合同要求**。

> ⚠️ 这两条同时"释放"了 `docs/14` §7.5 / §7.6 里那批 P1 预留状态
> （`courses` / `lessons` / `products` / `orders` / `payments` / `entitlements` …）——
> 它们**跟着本条件一起挂着**，不必单独再列一遍。

### 3.2 BL-06 · `--junitxml` 跨宿主编码

**理由：CI 正常，本地有别的途径。** `--junitxml` 是为解"跨宿主打印"才引入的（硬约定 I）：
与其赌控制台代码页，不如**绕开控制台**写文件。但当前 CI 全绿、本地要看结构化结果时
直接读 pytest 输出 / 覆盖率文件即可 —— **引入它是在解决一个还没出现的问题**。

**重启条件**：本地需要**机器可读**的用例结果（例如要写"变异验证自动对账"这类脚本）。

---


### 3.3 BL-11 · `status='archived'`（业务归档）**没有 UI 入口** —— 明确不做

**来历**：BL-09 把 `QuestionForm` 的状态下拉框改成只读之后，`archived` 这个值
**在界面上再也设不出来了**（原先它是唯一入口）。这是"移除一条旁路"的**必然副作用**，
不是漏改 —— 所以按硬约定 P 明确记下，而不是让它悄悄变成一条谁都不知道的隐性变化。

**理由（不做）**：`archived`（业务归档，与 `is_deleted` **正交**）在 UI 上与
「软删除（`DELETE`）」的**用户语义重合** —— 都是"让这道题不再出现在默认列表、不再被组卷引用"。
当前只有一个真实场景，用软删除即可，不需要两个入口表达同一件事。

⚠️ **后端仍然保留这个状态且仍可写入**（`PUT` 的 `status` 入参在本批**没有**移除），
所以它**不是死状态**（硬约定 F：可达 ⟺ 有任何接口能写入它）——
只是"从界面进不去"。这一点在 BL-09 后半段移除 `PUT` 的 `status` 时**必须重新评估**：
那时它才真的只剩读路径。

**重启条件（★ 2026-09-25 收紧：不是"要重新评估"，是"必须做决定"）**：

> **BL-09 后半段移除 `PUT` 的 `status` 入参时，必须当场三选一，不许"再看 / 暂缓 / 待定"。**
>
> | 选项 | 前提 | 动作 |
> |---|---|---|
> | **① 删掉 `archived`** | 确认它与软删除语义重合 | 从 `QStatus` / CHECK 约束 / 前端常量里一起移除（**要 migration**） |
> | **② 补显式入口** | 确认 `archived` 有**独立**语义（如"下架但保留审计可见性"） | 加 `POST /admin/questions/{id}/archive`，**不要塞回编辑表单** |
> | **③ 明确归软删除** | 它本就是软删除的重复表达 | 从状态机里移除，文档写明"归档 = 软删除" |
>
> **为什么必须"做决定"而不是"再看"**：BL-11 这条记录**本身**就是"移除一条旁路"的**副作用**——
> 它是被我们的改动**制造**出来的，不是本来就存在的待办。将来触发时如果只写"重新评估"，
> 它就永远悬着：**和"以后想起来"只差一层皮**（硬约定 P 要消灭的正是这个）。
> ⇒ 触发时刻**必须有一个确定的结果**，三者都可以，**唯独"没有结果"不行**。

## 4. BL-07 的执行方案：CI 覆盖率数字 vs 本地

**为什么现在做不了**：本地 **领先远端 4 个提交**（`45e48a7` 起，含 S2 前端与 docs/20 定稿），
CI 只看得到 `6dfc929` —— **现在对账，比的是两批不同的代码**，
结论无论正反都不可用（硬约定 J：先确认"你量的是哪一个"）。

**怎么比**（下次 CI 跑完，一次做完）：

| 步骤 | 动作 |
|---|---|
| 1 | 记下 CI 里的 `commit=` / `run=`（CI 步骤已打印，见坑 56） |
| 2 | 取 CI 的 `coverage report` 输出（或 HTML 页里嵌的 step 输出），记 **TOTAL 行 + 语句数 + 未覆盖数** |
| 3 | 在**同一个 commit** 上本地复跑一次，记同样三个数 |
| 4 | 比对 |

**判据（两条分支都要有结论，不许"差不多"）**：

| 结果 | 含义 | 动作 |
|---|---|---|
| **一致** | 说明"**只加字典键不增语句数**"这个判断是对的（`docs/20` §14.4：那 4 处 `origin_label`），也说明本地与 CI 的采集口径没有分叉 | 记档，**不需要动作** |
| **不一致** | 说明 §14.4 那句"**顺手改后端**"的处置有问题 → **去查**：是语句数变了（判断错），还是采集口径分叉（新坑） | 按坑 57 / 55 的路子排查（先看**元数据**，再看内容） |

> ⚠️ **比对时必须对齐 commit**。同一天的两次 run 输出可能逐行相同，
> 无法追溯来源的"证据"在推理上等价于"凭印象报的数字"（坑 56）。

### ⚠️ §4.1 一个已经实测到的**假红陷阱**：本地 `.coverage` 是**残留文件**

2026-09-25 侦察时发现：仓库根躺着一个 **9-24 17:07 的 `.coverage`**（不是本批产生的）。
直接拿它跑 `coverage report` 得到 **4567 / 218 → 95.23%**，
而权威数字是 **4567 / 210 → 95.40%**（差额 **8 行**，全在 `stats_service.py`）。

```
$ python -m coverage report --data-file .coverage     # ← 用"手边那个文件"
TOTAL                                        4567    218    95%
```

⇒ **若把它当成"本地值"去和 CI 比，会得到"不一致"的结论，然后去查一个不存在的问题**
（"顺手改后端"那句话明明是对的）。**这就是假红**（硬约定 J 的反面）。

**防法（写进步骤，不靠记性）**：

- 比对前**必须确认 `.coverage` 的修改时间**（`ls -la .coverage`）**晚于**目标 commit 的本地测试运行；
- 唯一可靠的做法：**在目标 commit 上重新跑一遍**，再用新生成的数据文件读数；
- ⚠️ 这条与坑 56（证据的身份）**同源**：**先问"这个数字来自哪一次运行"**，再谈它等于几。

---


### 4.2 BL-09 的依赖侦察结果（2026-09-25 已做完，动手时不用再查）

用户裁定是「先查依赖再移除：**不传就直接移除；传就先改前端**」。查完的结论：

| 查什么 | 结果 |
|---|---|
| 前端传不传 `status`？ | **传** —— `src/lib/question.ts:377` `if (before.status !== d.status) patch.status = d.status;`，且 `QuestionForm.tsx:272` 有状态下拉框（列了全部 5 个状态） |
| 测试有没有测「PUT 改 status」？ | **没有** —— 全部测试里对题目的 PUT **只有 1 处**（`tests/test_admin_v4.py:713`），payload 是 `{"version": 1}`。⚠️ 注意别把那处 **GET 列表筛选**的 `"status": "published"`（`test_admin_v4.py:270`）误读成 PUT 的 body |

⇒ **测试不构成阻碍，前端构成** ⇒ 必须先改前端（BL-09 的触发条件因此定成"改表单时"）。

⚠️ 还有一个**本批新加的用例**依赖这条旁路：`tests/test_question_review.py::test_archived_status_gets_its_own_hint`
用 `PUT` 把 status 改成 `archived`。移除 `status` 入参后**它必须一起改**（改用别的方式造 archived 态的题）。

### 4.3 BL-10 的来历：门禁"**能覆盖**"与"**能过**"是两件事（2026-09-25 实测）

用户给的判断标准：**存量 ≤ 20 条 → 顺手清掉并纳入门禁；> 20 条 → 记待办、独立成批**。
理由（用户原话）：**范围扩了但恒红，等于没有门禁** —— 硬约定 H 的同一逻辑。

实测两个门禁的存量差别很大，因此**不能一刀切**：

| 门禁 | 原范围 | 全仓存量 | 决定 |
|---|---|---|---|
| `ruff check` | `apps/api` | **6 条**（F401 × 2 / F841 × 4）→ **≤ 20** | ✅ **清零 + 扩到 `.`**（本批做） |
| `ruff format --check` | `apps/api` | **29 个文件待格式化**（89 已合规）→ **> 20** | ⛔ **不扩**，记 BL-10 |

**6 条的分布**（说明"盲区"确实存在，不只是 `tools/`）：

```
db/seed/gen_seed_questions.py      F841 × 3   ← 你的直觉对：不止 tools/
tools/local-verify/cdp_browser.py    F401 × 1
tools/local-verify/probe-import-pipeline.py F401 × 1
tools/local-verify/seed-questions.py F841 × 1
```

⚠️ **这 6 条里有 3 条不是"无脑删"**（F841 有两种：废代码 / **漏用**）：

| 位置 | 性质 | 处置 |
|---|---|---|
| `gen_seed_questions.py:790` / `:867` | `chapter = CH_ID[...]` 结果无人使用 —— 但**裸查表本身是个 fail-fast 校验** | 改成**显式** `if ... not in CH_ID: raise KeyError(...)`，**保留 fail-fast**（直接删会从"当场炸"变成"生成出悬空章节引用"） |
| `gen_seed_questions.py:1071` | `progress = len(...)/max(...)` 全文件只出现 1 次 → **纯废代码** | 删 |
| `seed-questions.py:123` | `out = run(...)` —— **`run()` 有副作用**（真的去跑生成器） | 只去掉赋值 `out =`，**保留调用**（直接删整行会把"生成"这一步一起删掉） |
| 两个 F401 | 真的没用到 | 删 |

## 5. 检查点：每批收尾扫一遍

**动作**：提交前，打开本文件，**从上到下逐条读"触发条件"这一列**，问一句
"**它现在成立了吗？**"

- 成立 → 本批处理（或明确改期并把理由写进本文件）
- 不成立 → 跳过，**但要看一眼"状态"那列是否已经过期**（例：某个 ⚪ 的前置其实已经上线了）

**已写进** `apps/admin/docs/B端联调坑.md` 附录「一批交付收尾的固定动作」**§8**。

---

## 6. 一页速览

```
规则（硬约定 P）：待办 = 编号 + 触发条件 + 检查点。三缺一 → 要么现在做，要么写"不做 + 理由"
                "以后想起来"不是待办，是逃避
                ⚠️ 写完触发条件，回头查一次"它现在是不是已经成立"（否则就是假条件 / BL-04）

进行中   BL-01a  questions 审核链               ← **前后端都已落地**
待执行   BL-07   CI 覆盖率 vs 本地对账           ← 阻塞在推送（本地领先若干提交）
         BL-09   移除 PUT 的 status 入参          ← **前端已做**；后端入参移除待做
未触发   BL-01b  comments 审核四件套             ← C 端评论区上线
         BL-01c  主观题人工批改三字段            ← C 端作答/批改批（与 unpublish 校验同批）
         BL-02   统计看板 S3                     ← 有真实作答数据
         BL-04   per-role 落地页                 ← 再次改 MODULE_ENTRIES 顺序（已 1 次）
         BL-05   部署前跑 build                  ← 任何部署前
         BL-08   审核权责独立                    ← 第 8 个角色 / 权限不再整包发放
         BL-10   format 门禁扩到全仓             ← 风格统一第二步（存量 29 文件）
         BL-12   next build 纳入 CI              ← 抽 packages/api-core 之前（§9.2.3 选 A）
         BL-13   登出对 token 即时生效            ← C 端实现登出时
         BL-14   daily 口径统一                  ← 做每日一练时（含"每日唯一"靠什么保证）
不做     BL-03   内容运营/课程/支付              ← 边际价值低
         BL-06   --junitxml 编码                 ← CI 正常，本地有别的途径
         BL-11   archived 状态无 UI 入口         ← 与软删除语义重合

检查点：每批收尾（B端联调坑.md 附录 §8）+ 扫「文档声称」清单（附录 §9 → docs/23）
```

> 📌 **BL-08 是"写方案时新产生的"待办**，来源 `docs/15` §8.3 的权限讨论。
> 记在这里而不写在方案里当一句注释 —— 因为方案文档**没有检查点**，
> 写在里面的"以后"就是这个文件存在的意义所要消灭的那种东西。
