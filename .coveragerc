# 覆盖率配置 —— 门禁整顿第二步（Commit A）引入。
#
# ⚠️ 这里**刻意不写 `source`**：`source` 的相对路径是按**进程 cwd** 解析的，
# 而覆盖率是在 **API 进程**里采集的（见 `tools/local-verify/serve_fake_redis.py` 的
# `--coverage`），那个进程会把 cwd 切到 `apps/api`。把 source 写在代码里，
# 只有一处、且与切换 cwd 的动作挨着，不会出现"换个目录就跑不起来"。
# 本文件负责 `[run]` 的采集方式与 `[report]` 的展示口径 + **门槛**。

# ---- ★★ 采集方式：必须启用 greenlet 支持（2026-09-21 补，见下方长注释）----
#
#     concurrency = greenlet
#
# 不写这一行，覆盖率数据**是错的**（不是"偏低"，是**错的**）：
#   · 同一个函数里，DB await 之前的行**有**记录，之后的行**全部丢失**
#   · 同时还会把没执行的分支记成执行（虚高）—— 两个方向都错
#
# **为什么**：本项目的 DB 走 SQLAlchemy async（`create_async_engine` + `AsyncSession`），
# 而 SQLAlchemy async 的**每一个** DB 调用都经 `greenlet_spawn()` **切一次 greenlet**。
# coverage 默认 `concurrency = thread` 不感知 greenlet 的栈切换 ——
# 切回来的那一刻追踪器手里的"当前帧"已经指错了，此后该帧的 line 事件全部作废。
#
# **实测证据**（`tools/local-verify/_probe_greenlet.py`，同进程、只改这一个变量）：
#
#     默认 thread ： auth.py:76(注册路由 return) = False   deps.py:117(取权限) = False
#                   deps.py 90-130 覆盖 = [...111, 121, 124]        ← 111 之后的 112-118 全丢
#     greenlet    ： auth.py:76 = True            deps.py:117 = True
#                   deps.py 90-130 覆盖 = [...111, 112, 114, 117, 118, 121, 124]
#
#     判断这两行"必然执行"的依据：用例断言的**就是** `auth.py:76` 的返回值；
#     `deps.py:117` 在 `current_user` 里，每个带鉴权的请求都会跑。
#     在修正前，它们**全被记为未执行** —— 也就是说修正前的覆盖率数字**不能用来定门槛**。
#
# ⚠️ **为什么三种追踪内核都试过才想到这一层**：默认 / pytrace / ctrace 结果**完全一致**，
#     说明问题不在"用哪个内核"，而在"内核与外部的 greenlet 栈切换没有对接"。
#     排错顺序上的教训记录在 `apps/admin/docs/B端联调坑.md` 坑 54。
#
# 依赖：`greenlet` 由 `sqlalchemy[asyncio]` 带进来（见 `apps/api/requirements.txt`），
#      `run-smoke.ps1` 的前置检查已把 `greenlet` 列入必须可导入的模块。

[run]
concurrency = greenlet

[report]
# ---- 门槛 ----
#
# ⚠️⚠️ **2026-09-21 更正：下面这套 2026-09-20 的实测数字全部作废。**
#
#     作废原因：采集配置缺了 `[run] concurrency = greenlet`（见文件顶部长注释）。
#     同一个 API 进程、同一套用例、**同一个分母（4305 语句）**，只改这一个配置：
#
#         作废口径：  未覆盖 1476  →  65.64%
#         修正口径：  未覆盖  579  →  86.55%
#
#     差额 897 条是**测量假象**，不是"新补的测试"。修正后的分布：
#
#     services     2286 语句   306 未覆盖   86.6%
#     (顶层)        222 语句   192 未覆盖   13.5%   ← cli.py 占 190，见下
#     core          398 语句    54 未覆盖   86.4%
#     schemas       925 语句    14 未覆盖   98.5%
#     db            152 语句     9 未覆盖   94.1%
#     api           322 语句     4 未覆盖   98.8%
#     ─────────────────────────────────────────────
#     TOTAL        4305 语句   579 未覆盖   86.55%
#
#     （留作记录的旧数字：1479 / 65.64%；services 1153 / 49.6%。）
#
# ⚠️ 仍然成立的一条判断：**`services` 层的缺口不是"未来批次还没写的代码"**，
#     而是**已有代码里没被测试打到的部分**（错误分支 / 边界处理 / 辅助方法）。
#     但**目标定错了**：修正后 services 是 86.6%，不是 49.6% ——
#     "补到 ≥ 60%" 这条线**本来就已越过 26 个点**。
#
# 📄 完整分类清单（A/B/C 逐条）与三处采集边界的说明：`docs/18-覆盖率缺口诊断.md`

# ---- 当前门槛 65 的来历（当时的取舍，留作记录）----
#
# 用户原本要求「实测值 +5~10 个百分点」（65.64 + 5.4 ≈ 71），但 **71 > 65.64** ——
# 门槛一落地 `coverage report` 就必然非 0 退出，`run-smoke.ps1` 不再"从零库全绿"、
# CI 也白不起来。两条验收要求在数学上不能同时成立，
# 于是取「**恒红的门禁等于没有门禁**」（硬约定 H）这一条原则，
# 把门槛定在**实测值下方一点点**作为**棘轮**：掉 1 个点以上就红。
#
# 用户 2026-09-20 裁定：**接受 65%**，并明确"当时的表述不严谨"。

# ---- 上一档门槛 93.10（2026-09-23 裁定，**补测批结束 → C 端阶段的第一次抬升**）----
#
# 来历（三跳，每跳都留了数字）：
#   85     2026-09-22 按"贴 89.25 − 4.25"定的**临时值**
#   90.75  ⑤c-1/⑤c-2（`import_service` +60 用例）把实测推到 **91.75**（4293/354）→ 抬到 91.75 − 1
#   93.10  ⑤b/⑤c/⑤d 收尾后实测 **95.10**（4290 语句 / 210 未覆盖）→ 本次抬升
#
# ⚠️ **本次同时改了"减几"**：从「实测 − 1」改成「**实测 − 2**」。
#
# 依据（用户 2026-09-23 的判定问题：「门槛 94.10 下，C 端加代码时 CI 会不会频繁撞红？」）：
#
#   设新增 X 条语句、其中覆盖率 c，当前已覆盖 C₀ = 4080、总数 T₀ = 4290。门槛 p 下要求
#
#       (C₀ + c·X) / (T₀ + X) ≥ p        ⇒   X ≤ (C₀ − p·T₀) / (p − c)
#
#   · 若 c > p：覆盖率**上升**，永不因此撞红（新代码测得比门槛好，是加分项）；
#   · 若 c < p：覆盖率**下降**，且 X 有上界 —— 上界就是"这一批最多能加多少未测代码"。
#
#   代入 p = 94.10（= 实测 − 1）与 p = 93.10（= 实测 − 2），取 c = 0.80（一批新代码的
#   偏保守估计：服务层 + 路由 + schema，先落地后补测试）：
#
#       p = 94.10 → X ≤ (4080 − 0.941×4290) / (0.941 − 0.80) = 43.1 / 0.141 ≈ **305 条**
#       p = 93.10 → X ≤ (4080 − 0.931×4290) / (0.931 − 0.80) = 86.0 / 0.131 ≈ **656 条**
#
#   **C 端一个批次（服务 + 路由 + schema + 校验）很容易超过 305 条语句**，而 656 条能容下
#   第一批。⇒ **判定为"会频繁撞红"**，按用户的判据取「**实测 − 2**」。
#
#   ⚠️ 反过来也核过：**不能取 − 3**。−3（92.10）的上界是 ≈1065 条，余量过宽，
#      覆盖率可以悄悄滑到 92% 而不吭声 —— 门禁还在，但已经量不出"退步"了。
#
#   ★ 另有一条独立理由支持 93.10 仍是"真门禁"：**它高于补测批开始前的 92.59**
#      （⑤d 实测）。也就是说，真回退到那个水平仍会被抓 —— 门槛没有低到失去意义。
#
# ⚠️ **这条规则不是永久降级**：`−1` 是"紧贴实测 + 留 1 个点抖动"，
#      适合"存量代码 + 补测试"的阶段；`−2` 是为"**大量新代码以批次落地**"的阶段准备的。
#      等 C 端走上正轨（新代码自带测试、覆盖率重新爬升）时，按同一张表把 `−2` 调回 `−1`。
#      判断依据与本次相同：**把预期的新增语句数代入上面那个不等式，看 X 的上界够不够一批。**
#
# ⚠️ **不取整到 90 / 93**：取整会把 0.10 个点的棘轮白白丢掉，
# 而"门槛 = 实测 − k"这条规则的意义正是让门槛紧贴实测、同时保留 k 个点的抖动余量。

# ---- ★★ 当前门槛 94.40（2026-09-24 裁定，**S1-b 完成后第一次抬升**）----
#
# 来历（四跳，每跳都留数字）：
#   85     2026-09-22 按"贴 89.25 − 4.25"定的临时值
#   90.75  ⑤c-1/⑤c-2 把实测推到 91.75（4293/354）→ 91.75 − 1
#   93.10  ⑤b/⑤c/⑤d 收尾后实测 95.10（4290/210）→ 首次启用「实测 − 2」
#   94.40  **S1-b（统计看板 HTTP 层）后实测 95.40（4567/210）→ 95.40 − 1**   ← 本次
#
# ★ 观察（用户 2026-09-24 要求记下）：**这是 93.10 生效后第一次触发棘轮。**
#   「实测 ≥ 门槛 + 2」这条规则自 2026-09-23 启用以来经过**四次**判定：
#     ① ⑤d  实测 92.59，门槛 90.75 → 92.59 < 92.75 → **不动**（挡下过早抬）
#     ② ⑤b  实测 95.10，门槛 90.75 → 95.10 ≥ 92.75 → **动**（→ 93.10，同时把减数改成 2）
#     ③ 现在 实测 95.40，门槛 93.10 → 95.40 ≥ 95.10 → **动**（→ 94.40）
#     ④ **2026-09-25 Batch 9 审核链：实测 95.42，门槛 94.40 → 95.42 < 96.40 → 不动**
#        （涨了 0.02pp，离"门槛 + 2"还差 0.98pp —— 这正是"不因小涨就抬"该有的表现）
#   ⇒ 它**既不会一直抬，也不会永远不抬** —— 这才是棘轮该有的行为：
#      **抬的时机由「实测与门槛的距离」决定，不由「批次到了」决定。**
#
# ★★ 顺带回答「下一步 S1-c（前端）会不会撞红」—— **前提要先纠正**：
#   `coverage run --source app` 的采集范围是 **`apps/api/app/`（后端 Python）**；
#   前端 `apps/admin` 的 `package.json` 里**没有**覆盖率脚本（只有 build / lint / typecheck / format）。
#   ⇒ **纯前端的 S1-c 不进这个分母，不可能撞红。**
#      （原假设"若撞红 → 说明前端覆盖率低于 k=2 的假设"，前提不成立：前端压根没被量。）
#
#   ⚠️ 真正要盯的是另一件事：**S1-c 若顺手改了后端**，那部分新增 Python 行会计入分母。
#      同一张不等式（p = 0.944、c = 0.80、C₀ = 4357、T₀ = 4567）：
#          X ≤ (4357 − 0.944 × 4567) / (0.944 − 0.80) = 45.8 / 0.144 ≈ **318 条**
#      ⇒ 后端还能容下约 **318 条**"未测的新代码"；新代码若自带测试（c ≥ 0.944）则永不撞红。
#
# ⚠️ **减数仍是 2，不是 1**：`−2` 是为"**大量新代码以批次落地**"的阶段准备的。
#    本条门槛是"实测 − 1"，但**规则里比较用的是"门槛 + 2"** —— 两者别混。
#    等 C 端走上正轨（新代码自带测试、覆盖率稳定爬升）再按同一张表调回 `−1`。

fail_under = 94.40

# ---- ★★ 操作规则：每次"顺手改后端"之前，先算一次余量（2026-09-25 定）----
#
# 上面那节把 318 条算出来了，但它藏在"**S1-c 会不会撞红**"这个**批次特例**里 ——
# 下一个人遇到"顺手改后端"时不会想到来这儿找。所以把结论**升成规则**：
#
#     X ≤ (C₀ − p·T₀) / (p − c)
#         X = 这一批能容下的「未测新代码」语句数（上界，超过就撞红）
#         C₀ / T₀ = 当前已覆盖语句数 / 总语句数      p = `fail_under`
#         c = 这批新代码**自带测试**的覆盖率（服务层+路由+schema 先落地后补测试，取 0.80）
#
#   · **纯前端改动 → 不适用，不必算**。`coverage run --source app` 只扫 `apps/api/app/`，
#     `apps/admin` 的 `package.json` 里**没有**覆盖率脚本 —— 前端改多少都不进分母。
#   · **动了后端 → 必须算**，把当前 C₀ / T₀ / p 代进去，别凭"应该够吧"。
#
# 当前快照（2026-09-25 实测）：p=0.944、c=0.80、C₀=4458、T₀=4672
#     → X ≤ (4458 − 0.944×4672) / (0.944 − 0.80) = 47.6 / 0.144 ≈ **330 条**
#
#   ⇒ 一句话：**后端还能容约 330 条未测的新代码**。新代码若自带测试（c ≥ p）**永不撞红**，
#     所以这个数只对"先落地、后补测试"的写法有意义 —— 它不是"还剩多少额度随便花"。
#
#   ★ **本规则第一次实战（2026-09-25，Batch 9 审核链）**：新增 **+105 语句**，
#     其中 9 条未覆盖（c ≈ **0.914**，高于假设的 0.80）→ 覆盖率 **95.40% → 95.42%**（略升），
#     余量 **318 → 330 条**（也升了 —— 因为 C₀ 涨得比 p·T₀ 多）。
#     全程没有撞门禁，符合预期；**结论是"这个算式在真实批次上算得准"**，不是"余量还很多"。
#
# ⚠️ **318 是快照，不是常数**：门槛是棘轮，覆盖率一涨，余量就变小。
#    把它当常数用 = 给了一个**会过期的安全余量**（与"旧门槛 65 掉 21 个点还是绿的"同族）。
#    ⇒ 规则是"**现算**"，快照只是"上次算出来是多少"。

# ---- ★ 抬升规则（**动态**，可判定，不靠"记得回来改"）----
#
#     触发：实测 ≥ 门槛 + 2
#     动作：门槛 = 实测 − k
#
#   **k 是阶段相关的，不是常数**（2026-09-23 起）：
#     k = 1 —— "存量代码 + 补测试"阶段（紧贴实测，留 1 个点抖动）
#     k = 2 —— "**大量新代码以批次落地**"阶段（本项目 2026-09-23 进入 C 端时改用这个）
#   切换依据是可算的：把「预期一批新增语句数」代入上方「减几」小节的不等式，
#   看 X 的上界够不够装下一批。**别凭感觉调 k。**
#
# ⚠️ **为什么不用固定的「实测 ≥ 90%」**（2026-09-23 用户指出的缺陷）：
# 固定阈值**只在第一次触发时成立**。抬到 90.75 之后，下次"实测 ≥ 90%"仍然成立 ——
# 规则会退化成"**每次小涨就抬**"，门槛一路贴着实测值走，
# **正常开发（不补测试）一动就红**。那就不是棘轮，是陷阱。
# 写成相对门槛的「+2」之后，它天然带"留 1 个点余量"的性质：
# 只有涨到 **门槛 + 2** 以上才动，动完仍比实测低 1 个点。
#
# 算例（**前两条是实战记录，不是推演**；2026-09-23 逐条核对过）：
#
#     实测 91.75% / 门槛 85%     → 91.75 ≥ 87     ✅ → 抬到 90.75%     （⑤c-1/2，k=1）
#     实测 92.59% / 门槛 90.75%  → 92.59 < 92.75  ❌ → **不动**（⑤d 后）
#         ★ 这一条是这套规则存在的理由之一：**它挡下了一次过早抬升** ——
#           旧写法"实测 ≥ 90%"在这里会成立，把门槛抬到 91.59、余量压到 1.0pp，
#           而当时补测批还没跑完，紧接着的开发一动就会红。
#     实测 95.10% / 门槛 90.75%  → 95.10 ≥ 92.75  ✅ → 抬到 93.10%     （⑤b/⑤c/⑤d 收尾，k=2）
#     实测 94%    / 门槛 93.10%  → 94    ≥ 95.10  ❌ → 不动
#     实测 96.5%  / 门槛 93.10%  → 96.5  ≥ 95.10  ✅ → 抬到 94.50%     （k=2）
#
#     ⇒ 规则既**挡得住过早抬**，也**抬得及时**，两点都不需要人记。
#
# ⚠️ **为什么必须写成"条件 + 动作"而不是"达到后改成 X"**：
# 后者依赖某个人的记忆 —— **没有任何交付流程会因为"当时说过达到后要改"而停下来**，
# 结局就是门槛变成永久值，而**棘轮一旦不动，它就从"门禁"退化成一个数字**。
# 写成可判定的条件之后，"每批收尾跑一次 `coverage report` 对一下"
# （见 `apps/admin/docs/B端联调坑.md` 末尾「一批交付收尾的固定动作」§5）就能直接执行。
#
# **刻意不设 95%**：把门槛定在"现在做不到"的位置，唯一效果是逼人写凑数的测试
# 去把数字抬上来，那是把覆盖率变成装饰品。

# 默认不打印逐文件明细（总览足够做门禁；要看细节加 --show-missing）
show_missing = false
skip_covered = false

# 这些行**没有可执行语义**，算进分母只会压低数字并诱使人为它写无意义的测试。
exclude_lines =
    pragma: no cover
    if TYPE_CHECKING:
    if __name__ == .__main__.:
    \.\.\.$
    raise NotImplementedError
