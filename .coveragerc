# 覆盖率配置 —— 门禁整顿第二步（Commit A）引入。
#
# ⚠️ 这里**刻意不写 `source`**：`source` 的相对路径是按**进程 cwd** 解析的，
# 而覆盖率是在 **API 进程**里采集的（见 `tools/local-verify/serve_fake_redis.py` 的
# `--coverage`），那个进程会把 cwd 切到 `apps/api`。把 source 写在代码里，
# 只有一处、且与切换 cwd 的动作挨着，不会出现"换个目录就跑不起来"。
# 本文件负责 `[report]` 的展示口径与**门槛**。

[report]
# ---- 门槛 ----
#
# **实测依据（2026-09-20 首次测量，不是拍脑袋）**：
#
#     44 个文件 / 4305 条语句 / 未覆盖 1479  →  **65.64%**
#
#     schemas       925 语句   14 未覆盖   98.5%
#     db            152 语句   10 未覆盖   93.4%
#     core          398 语句   59 未覆盖   85.2%
#     api           322 语句   51 未覆盖   84.2%
#     services     2286 语句 1153 未覆盖   49.6%   ← 大头，见下
#     (顶层)        222 语句  192 未覆盖   13.5%   ← cli.py / main.py，只有启动路径被跑到
#
# ⚠️⚠️ **`services` 的 49.6% 不是"未来批次还没写的代码"**（这个描述我一开始写错了）：
#     services 层是**已经写完、正在跑的代码**。那 1153 条未覆盖语句是
#     **现有代码里没被测试打到的部分** —— 错误分支、边界处理、辅助方法。
#     **区别很重要**：它意味着**补测是现在就能做的事**，不需要等任何未来批次。
#
# ---- 为什么是 65 而不是 71（当时的取舍，留作记录）----
#
# 用户原本要求「实测值 +5~10 个百分点」（65.64 + 5.4 ≈ 71），但 **71 > 65.64** ——
# 门槛一落地 `coverage report` 就必然非 0 退出，`run-smoke.ps1` 不再"从零库全绿"、
# CI 也白不起来。两条验收要求在数学上不能同时成立，
# 于是取「**恒红的门禁等于没有门禁**」（硬约定 H）这一条原则，
# 把门槛定在**实测值下方一点点**作为**棘轮**：掉 1 个点以上就红。
#
# 用户 2026-09-20 裁定：**接受 65%**，并明确"当时的表述不严谨"。
#
# fail_under = 65   ← 实测 65.64%，留 0.64pp 抖动余量

# ---- ★ 抬升触发条件（可判定，不靠"记得回来改"）----
#
#     触发：实测总覆盖率 ≥ 70%      **或**   services 层 ≥ 60%
#     动作：把 fail_under 改成 71
#
# ⚠️ **为什么必须写成"条件 + 动作"，而不是"达到后改成 71"**：
#     后者依赖某个人的记忆。**没有任何交付流程会因为"当时说过达到后要改"而停下来** ——
#     结局就是 65% 变成永久门槛，而**棘轮一旦不动，它就从"门禁"退化成一个数字**。
#     写成可判定的条件之后，"每批收尾跑一次 coverage report 看当前数字"
#     （见 `apps/admin/docs/B端联调坑.md` 末尾「一批交付收尾的固定动作」）就能直接对照执行。
#
# 再往上怎么走：越过 71% 之后同样按这个套路 —— 门槛提到「实测值 − 1」，
# 并重新写一条新的"下一档 + 触发条件"。
#
# **刻意不设 80%**：把门槛定在"现在做不到"的位置，唯一效果是逼人写凑数的测试
# 去把数字抬上来，那是把覆盖率变成装饰品。

fail_under = 65

# 默认不打印逐文件明细（总览足够做门禁；要看细节加 --show-missing）
show_missing = false
skip_covered = false

# 这些行**没有可执行语义**，算进分母只会压低数字并诱使人为它写无意义的测试。
exclude_lines =
    pragma: no cover
    if TYPE_CHECKING:
    if __name__ == .__main__.:
    \.\.\.$
    raise NotImplementedError
